---
id: SPEC-008
tipo: ledger
epica: EPIC-002
---
# Ledger — SPEC-008 Despliegue del tick: pg_cron, Vercel Cron, variables y sincronizacion del calendario

## Resumen
- Fase: en-progreso (CA-3 reabierto el 2026-09-22: `cron:setup` falló en ejecución real, F-SPEC-008-10)
- Rama: `ft/SPEC-008-despliegue-del-tick`, y `ft/SPEC-008-cron-setup-array` para el arreglo de CA-3

## Matriz de criterios de aceptación
<!-- Escritores: sdd-implementador rellena Implementado y Test; sdd-verificador rellena Verif. y Estado. Nunca al revés. -->
<!-- Estados por CA: ✅ cerrado · ⚠️ parcial/con salvedad · 🚧 en curso · ❌ sin empezar · n-a -->
<!-- Un CA está ✅ solo cuando Implementado + Test + Verif. aplicables están en verde. Una salvedad se marca ⚠️, nunca ✅. -->
| CA | Implementado (fichero) | Test (fichero/caso) | Verif. | Estado |
|---|---|---|---|---|
| CA-1 | `src/db/client.ts` (`getSql()` memoizado, `import "server-only"` intacto), `src/app/api/ingest/tick/route.ts` (sin `let pool`, importa `getSql`), `src/db/schema.db.test.ts` (pasa a `getSql()`), `vitest.config.mts` (alias `server-only`, F-SPEC-008-3) | `src/db/client.test.ts` → «SPEC-008 CA-1 src/db/client.ts is lazy» (3 casos: importa sin `DATABASE_URL`, `getSql()` lanza `DATABASE_URL is not set`, dos llamadas `toBe` la misma instancia) | 2026-09-21 sdd-verificador, reproducido: `.env` retirado del árbol + `env -u DATABASE_URL -u API_FOOTBALL_KEY -u NEXT_PUBLIC_SUPABASE_URL -u NEXT_PUBLIC_SUPABASE_ANON_KEY -u SUPABASE_SERVICE_ROLE_KEY -u INGEST_TICK_TOKEN npx next build` → **salida 0** con la ruta `ƒ /api/ingest/tick` (`.env` restaurado, árbol limpio). `grep -c "let pool" src/app/api/ingest/tick/route.ts` → **0**; `src/db/client.ts` conserva `import "server-only"` y exporta solo `getSql`. Los 3 casos de `client.test.ts` son reales: `vi.resetModules()` por caso, `process.env` restaurado en `afterEach`, memoización afirmada con `toBe` y no con `toEqual`. | ✅ |
| CA-2 | `src/app/api/ingest/tick/route.ts` → `export const GET = POST`. Ningún fichero **preexistente** de `src/ingest/` modificado salvo `engine.test.ts`, que es CA-9 (`git diff main --name-status --diff-filter=M -- src/ingest`); los cuatro ficheros nuevos de esa carpeta (`cron.ts`, `cron.test.ts`, `salud.ts`, `salud.test.ts`) los piden CA-3 y CA-7 | `src/app/api/ingest/tick/route.test.ts` → «SPEC-008 CA-2 GET and POST on /api/ingest/tick» (3 casos: importa con `process.env = {}`, `GET` `toBe` `POST`, `runtime`/`dynamic`/`maxDuration`) | 2026-09-21 sdd-verificador. Unitario: `GET` `toBe` `POST`, `runtime`/`dynamic`/`maxDuration`, importación con `process.env = {}`. **Flujo real añadido por el verificador** contra el build de producción servido con `next start -p 3187` y `.env`: `POST` sin cabecera → **401**, `GET` sin cabecera → **401**, `POST` con Bearer de igual longitud y distinto valor → **401**, `PUT` con el token bueno → **405**, `POST` con el token bueno → **200** `{"inWindow":0,"purge":"skipped","attempts":[]}`, `GET` con el token bueno → **200** con el mismo cuerpo. H-2 queda probado en ejecución y no solo por identidad de referencia. Ningún fichero **preexistente** de `src/ingest/` modificado salvo `engine.test.ts` (CA-9): `git diff main --name-status --diff-filter=M -- src/ingest` → solo `engine.test.ts`. Lo que falta de CA-2 es únicamente el mismo flujo contra el despliegue de Vercel (CA-6). | ✅ |
| CA-3 | `src/ingest/cron.ts` (`cronSecrets`, `findSecretIds`, `setupCronSecrets`, `TICK_URL_SECRET`, `TICK_TOKEN_SECRET`; la búsqueda pasa a array JS plano el 2026-09-22, F-SPEC-008-10), `tools/cron-setup.mjs`, `package.json` → `cron:setup`, `.env.example` → `INGEST_TICK_URL=` y `CRON_SECRET=` | `src/ingest/cron.test.ts` → 8 casos: Vault vacío → dos `vault.create_secret`; los dos presentes → dos `vault.update_secret`; el informe trae los dos nombres y ninguno de los dos valores; sin `INGEST_TICK_URL` / `INGEST_TICK_TOKEN` lanza nombrándola y sin emitir sentencia; `cron:setup` sale 1 sin variables y sin abrir conexión. **`src/ingest/cron.db.test.ts` (2026-09-22, contra la base real)**: `findSecretIds` como primera sentencia de un pool nuevo (el caso frío, que es el único que caza F-SPEC-008-10); el ciclo entero `creado` → `actualizado` con los valores correctos en `vault.decrypted_secrets`, en transacción con rollback y borrando antes las dos filas dentro de la tx; y ninguna huella del test en el Vault al terminar | 2026-09-21 sdd-verificador. `src/ingest/cron.ts` es puro (recibe `sql` y `env`, no abre nada) y `tools/cron-setup.mjs` es la cáscara. Los 8 casos muerden: el doble de `sql` registra el texto de cada sentencia, así que «dos `create_secret`» / «dos `update_secret`» se afirman por conteo sobre lo emitido, no por ausencia de excepción; «el informe no contiene ningún valor» se afirma con `not.toContain` de los dos valores; «lanza antes de abrir nada» comprueba además `calls` vacío. `cron:setup` se ejecuta de verdad con `spawnSync` en un cwd sin `.env` → salida 1, `stderr` nombra `INGEST_TICK_URL`, `stdout` vacío. `.env.example` gana `INGEST_TICK_URL=` y `CRON_SECRET=` sin valores. | ✅ |
| CA-4 | `supabase/migrations/20260921203746_ingest_tick_job.sql` (única migración nueva): `create extension if not exists supabase_vault`, `unschedule` condicional y `cron.schedule('ingest-tick', '30 seconds', …)` con `net.http_post`, `timeout_milliseconds := 55000`, `body := '{}'::jsonb` y URL/token leídos de `vault.decrypted_secrets` por nombre | `src/db/cron.db.test.ts` → «SPEC-008 CA-4 pg_cron job ingest-tick» (4 casos: imprime `extversion`; `supabase_vault` instalado; una sola fila `ingest-tick`, `active`, `schedule = '30 seconds'`; el `command` menciona `vault.decrypted_secrets` y `net.http_post`, y no contiene ni `Bearer sb` ni el valor de `INGEST_TICK_TOKEN`) | 2026-09-21 sdd-verificador, **contrastado contra `dev` por consulta propia, no contra el ledger**. Artefacto y test: una migración nueva (`git diff main --name-only -- supabase/migrations/` → 1 fichero), `supabase db push --dry-run` → `{"upToDate":true,"migrations":[]}`; `cron.job` tiene **una sola** fila `ingest-tick`, `active = true`, `schedule = '30 seconds'`; su `command` contiene `vault.decrypted_secrets`, `net.http_post` y `timeout_milliseconds := 55000`, y **no** contiene `Bearer sb` (`position` → 0) ni el valor de `INGEST_TICK_TOKEN`. **H-3 confirmado de primera mano**: `pg_cron 1.6.4` (con `pg_net 0.20.4`, `supabase_vault 0.3.1`, PostgreSQL 17.6) aceptó `'30 seconds'`; sobre `cron.job_run_details` conté **26 ejecuciones** entre `20:38:28.639Z` y `20:50:59.155Z` y calculé los **25 intervalos**: todos entre **30.013 s y 30.107 s**. El fallback de H-3 no hizo falta y el objetivo de mediana < 45 s de `vision.md` no depende de él. `select count(*) from ingest_attempts` → **0**. **Salvedad**: la mitad de flujo real del CA («~10 ejecuciones y todas `succeeded`; diez `200` en `net._http_response`») **no se cumple**: las 26 son `failed` con `null value in column "url" of relation "http_request_queue"` y `net._http_response` no tiene **ninguna** fila, porque `vault.decrypted_secrets` está vacío. Es decir: está probado que el job existe y dispara puntualmente cada 30 s, y **no** está probado que el cuerpo del `cron.schedule` llegue a encolar la petición. Se desbloquea con `npm run cron:setup`, que exige `INGEST_TICK_URL` (dato del titular, F-SPEC-008-2) — misma dependencia humana que CA-6. | ⚠️ |
| CA-5 | `vercel.json` (solo `crons`, `/api/ingest/tick`, `* * * * *`) | `src/arch/deploy.test.ts` → «SPEC-008 CA-5 vercel.json» + «SPEC-008 CA-5 the cron path carries the alias» (4 casos: un solo cron con esa ruta y horario; `vercel.json` no declara nada más; esa ruta tiene entrada en `outputFileTracingIncludes` con `./data/alias/**/*.json`; la ruta apunta a un `route.ts` que existe) | 2026-09-21 sdd-verificador. `vercel.json` es exactamente `{"crons":[{"path":"/api/ingest/tick","schedule":"* * * * *"}]}` y nada más (`Object.keys` → `["crons"]`). `next.config.ts` tiene `outputFileTracingIncludes["/api/ingest/tick"] = ["./data/alias/**/*.json"]`, comprobado a mano y atado por el test, que además lee la ruta **de `vercel.json`**: si alguien mueve el cron, el test cae de verdad. `CRON_SECRET` no se toca en `src/ingest/auth.ts` (`git diff main -- src/ingest/auth.ts` vacío) y el `GET` con Bearer responde 200 (ver CA-2), que es el camino exacto que usará Vercel Cron. **Salvedad**: la mitad de flujo real del CA (cron activo en el panel de Vercel, última ejecución 200, una invocación por minuto con `inWindow: 0` en los logs) **no se ha tocado**: exige el despliegue, misma dependencia humana que CA-6. | ⚠️ |
| CA-6 | Nada que implementar en el repo: son variables del panel de Vercel. **Bloqueado, F-SPEC-008-1** (sin CLI ni token de Vercel en este entorno) | n-a (verificación de flujo real) | 2026-09-21 sdd-verificador: **no verificado, y no simulado**. Comprobado por mí que no hay forma de hacerlo aquí: no existe `.vercel/`, `vercel` no está en el `PATH`, `VERCEL_TOKEN` no está en `.env`, y en el diff de la rama no hay ningún mock, stub ni respuesta de Vercel falseada (la única mención es el texto de F-SPEC-008-1). Tampoco hay ninguna URL de producción incrustada en `src/`, `tools/`, `vercel.json` ni la migración (los `marcador.gal` que aparecen son `User-Agent` preexistentes y valores de test). **Sí verificado, la mitad local del CA**: en mi propio build, `.next/server/app/api/ingest/tick/route.js.nft.json` **contiene** `data/alias/2026-27/api-football.json` (SPEC-006 CA-4, ADR-008 §8). Y el flujo 401/401/401/405/200/200 está probado contra el build de producción en local (CA-2), con `ingest_attempts` en **0** después de las llamadas. Queda literalmente pendiente de mano humana: las siete variables en Production, ninguna en Preview, y el 503 de un preview. | ❌ |
| CA-7 | `src/ingest/salud.ts` (puro: `tickSalud(input) → { ok, text }`, redacta secretos; **el 2026-09-22 el veredicto pasa a decidirse sobre una ventana corta y la lista de fallos se acota**, F-SPEC-008-13, con `SALUD_RECENT_MINUTES = 10` y `SALUD_FAILURES_SHOWN = 5` en `src/ingest/constants.ts`), `tools/tick-salud.mjs` (cáscara: `cron.job`, `cron.job_run_details` de la última hora, `net._http_response`, `ingest_attempts`, `alerts` sin resolver, `windowMatches`; sale 1 si `!ok`), `package.json` → `tick:salud` | `src/ingest/salud.test.ts` → «SPEC-008 CA-7 tickSalud» (6 casos: hora limpia → `ok: true`, seis bloques y `100%`; ejecución `failed` → `ok: false` nombrando el job; `failed` de hace 90 min → `ok: true`; intento `ok: false` → `ok: false` con su `error`; sin ninguna fila → seis bloques sin lanzar; el token en una fila sale como `[secreto]`). **Tres casos más (2026-09-22)**: hora con fallos viejos pero ventana corta limpia → `ok: true` con la coletilla «N problema(s) en la última hora»; intento fallido anterior a la ventana corta → `ok: true` y aun así visible en su bloque; cuarenta fallos → exactamente cinco líneas `FALLO`, las cinco más recientes, más `… y 35 más`, y el informe entero por debajo de 40 líneas. Comprobado que muerden: con la ventana a 60 min y sin tope caen los tres | 2026-09-21 sdd-verificador. `src/ingest/salud.ts` es puro de verdad: recibe `now` y las filas, no importa `src/clock.ts` ni `postgres`, no hay `new Date(`. Los 6 casos son los que pide el CA y ninguno es vacío: el de redacción mete el token en `error` **y** en `details` y afirma `not.toContain(TOKEN)` más la presencia de `[secreto]`. **Flujo real ejecutado por el verificador**: `npm run tick:salud` contra `dev` → **salida 1**, cabecera `tick:salud · 2026-09-21T20:53:26.196Z`, los **seis** bloques presentes (`jobs de pg_cron` con `ingest-tick 30 seconds activo`, `ejecuciones (última hora)` con `succeeded: 0%` y 30 líneas `FALLO`, `respuestas de pg_net`, `intentos de ingesta`, `alertas sin resolver`, `partidos en ventana: 0`) y el cierre `REVISAR: el tick no está sano`. Sobre esa salida, `grep -cF` de los **siete** valores de `.env` (incluidos `DATABASE_URL` y `DATABASE_PASSWORD`) → **0** en todos. El semáforo del viernes funciona y hoy está en rojo por la razón correcta. | ✅ |
| CA-8 | `.github/workflows/calendario-semanal.yml`: `schedule: '0 5 * * 2'` + `workflow_dispatch`; job `sync` (npm run calendario:sync, PR `chore/calendario-<fecha>` solo con diff, cuerpo con la salida de `formatSyncDiff`); job `load` en `push` a `main` con `paths: [data/calendario/**, data/alias/**]` (entrada `cargar` para lanzarlo a mano, F-SPEC-008-5) | `src/arch/deploy.test.ts` → «SPEC-008 CA-8 calendario-semanal.yml» (4 casos: martes 05:00Z y `workflow_dispatch`; sync + PR solo con diff en `data/calendario`/`data/alias`; load en push a `main` con los dos `paths` y `secrets.DATABASE_URL`; `secrets.API_FOOTBALL_KEY` y nunca la clave en el repo) | 2026-09-21 sdd-verificador: **verificado como artefacto, no en ejecución** (F-SPEC-008-6 es correcto: GitHub solo ofrece `workflow_dispatch` y respeta `schedule` para ficheros de la rama por defecto). Parseado por mí con Psych, no por `toContain`: `on.schedule[0].cron = '0 5 * * 2'`; `on.workflow_dispatch.inputs` = `temporada` (`2026-27`) y `cargar` (`no`); `on.push.branches = ['main']` y `on.push.paths = ['data/calendario/**','data/alias/**']`; job `sync` con `if: github.event_name != 'push' && inputs.cargar != 'si'` (6 pasos) y job `load` con `if: github.event_name == 'push' || inputs.cargar == 'si'` (4 pasos) — las dos expresiones son correctas para los tres disparadores. El cuerpo del PR lleva la salida de `formatSyncDiff`: comprobado que `tools/calendario-sync.mjs:112` hace `console.log(formatSyncDiff(...))` y el paso la captura con `tee sync-diff.txt`. `API_FOOTBALL_KEY` y `DATABASE_URL` vienen de `secrets`, nunca del repo. **Dos observaciones, O-1 y O-2 abajo.** Queda pendiente de mano humana, tras la fusión: dispatch limpio sin PR, dispatch con cambio forzado que abre PR, y `load` a mano contra `dev`. | ❌ |
| CA-9 | Sin código nuevo: `createEngineSweep` ya existía (SPEC-007) | `src/ingest/engine.test.ts` → «SPEC-008 CA-9 createEngineSweep» (4 casos: abre exactamente una `db.transaction` y antes de ella no hay sentencia; las cuatro consultas de SPEC-007 CA-9 una sola vez y con los dos `matchId`; `counts.matches === 2`; con ventana vacía no emite ninguna sentencia) | 2026-09-21 sdd-verificador: **el test afirma la propiedad, no la ausencia de excepción**, que era justo el riesgo. `fakeDb` hace `transactions.push(calls.length)` en cada `db.transaction`, y el caso afirma `expect(transactions).toEqual([0])`: eso es **exactamente una** transacción (longitud 1) y abierta **antes** de cualquier sentencia (posición 0) — una segunda transacción daría `[0, n]` y caería. El segundo caso afirma `calls` de longitud **4** y que las cuatro consultas de SPEC-007 CA-9 (`from matches`, `from decisions`, el `last heard` y el `from observations`) llevan cada una los **dos** `matchId` ordenados; el tercero, `counts.matches === 2`. O-1 de SPEC-007 queda cerrado de verdad. El cuarto caso documenta con honestidad que con ventana vacía la transacción se abre igual y no se emite nada (la guarda de ADR-009 §1 vive en `runTick`). | ✅ |
| CA-10 | `package.json` sin dependencias nuevas y solo dos scripts (`cron:setup`, `tick:salud`); una sola migración nueva; imports relativos con `.ts` en los ficheros nuevos | Cubierto por los gates y los greps de frontera (ver abajo) | 2026-09-21 sdd-verificador, **todo corrido por mí**. `env -u DATABASE_URL -u API_FOOTBALL_KEY -u NEXT_PUBLIC_SUPABASE_URL -u SUPABASE_SERVICE_ROLE_KEY -u INGEST_TICK_TOKEN npm run gates` → **salida 0**: Vitest **44 ficheros / 529 tests** en verde, `next build` con `ƒ /api/ingest/tick`. `npm run test:db` → **salida 0**: `db:push` `{"upToDate":true}` y Vitest **6 ficheros / 63 tests**. `git diff main --stat -- src/sources src/decide` → **vacío**. `grep -rnE "new Date\(|Date\.now\(" src/ingest src/raw src/decide` → **vacío**. Fuga de secretos: bucle `git grep -cF` **y** `grep -rlF` sobre el árbol con los **ocho** valores de `.env` (`API_FOOTBALL_KEY`, `DATABASE_PASSWORD`, `DATABASE_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `INGEST_TICK_TOKEN`, `SUPABASE_ACCESS_TOKEN`) → **0 coincidencias** en los dos, con `vercel.json` y la migración en el árbol. Ninguna URL de producción incrustada. `git diff main --stat -- package-lock.json` → **vacío** y el diff de `package.json` son **solo** las dos líneas `cron:setup` y `tick:salud`. `supabase/migrations/`: **un** fichero nuevo. Ningún import relativo sin `.ts` en los ficheros nuevos de la rama (barrido automático). Árbol limpio al terminar. | ✅ |
### Evidencia de ejecución (sdd-implementador, 2026-09-21)
- **Gates, sin `.env` en el árbol y con el entorno desnudo** (`mv .env` fuera + `env -u DATABASE_URL -u API_FOOTBALL_KEY -u NEXT_PUBLIC_SUPABASE_URL -u SUPABASE_SERVICE_ROLE_KEY -u INGEST_TICK_TOKEN npm run gates`): **salida 0**. `vitest run` → **44 ficheros, 529 tests, todo verde**; `next build` → `ƒ /api/ingest/tick` (CA-1).
- **`npm ci` en el mismo entorno desnudo**: salida 0.
- **`npm run test:db`**: **salida 0**. `db:push` → `{"upToDate":true,...}`; `vitest --config vitest.db.config.mts` → **6 ficheros, 63 tests** en verde.
- **`supabase db push --dry-run --db-url "$DATABASE_URL"`** → `{"upToDate":true,"dryRun":true,"migrations":[]}`.
- **pg_cron (H-3): `extversion = 1.6.4`, y aceptó `'30 seconds'` sin discusión.** El fallback de H-3 (job por minuto con dos `net.http_post` y un `pg_sleep(30)`) **no ha hecho falta**. Medido en `cron.job_run_details`: 15 ejecuciones entre `20:38:28.639Z` y `20:45:28.965Z`, separadas exactamente 30 s.
- **pg_net y Vault**: `pg_net 0.20.4`, `supabase_vault 0.3.1`, PostgreSQL 17.6. `net.http_post` vive en el esquema `net` y `net._http_response` también, como dan por hecho CA-4 y CA-7.
- **Estado del job hoy**: programado y disparando cada 30 s, pero las 15 ejecuciones son `failed` con `null value in column "url" of relation "http_request_queue"`, porque el Vault está vacío: falta `npm run cron:setup`, que a su vez necesita `INGEST_TICK_URL` en `.env` (F-SPEC-008-2). `select count(*) from ingest_attempts` → **0** (ninguna petición ha salido y estamos fuera de ventana).
- **Alias trazado en el build** (mitad local de CA-6): `.next/server/app/api/ingest/tick/route.js.nft.json` contiene `data/alias/2026-27/api-football.json`.
- **Frontera (CA-10)**: `git diff main --stat -- src/sources src/decide` → **vacío**. `grep -rnE "new Date\(|Date\.now\(" src/ingest src/raw src/decide` → **vacío**. `git grep -F` de `INGEST_TICK_TOKEN`, `API_FOOTBALL_KEY`, `SUPABASE_SERVICE_ROLE_KEY` y `DATABASE_PASSWORD` → **sin coincidencias** (con `vercel.json` y la migración en el árbol).
- **`npm run tick:salud` contra `dev`**: imprime los seis bloques, marca las cinco ejecuciones `failed` de la última hora y sale **1** con `REVISAR: el tick no está sano`. Es el comportamiento correcto: el job existe y falla porque le falta el secreto.

## Veredicto del verificador
<!-- GREEN/RED + fecha + resumen. Lo escribe SOLO sdd-verificador. -->
**GREEN CONDICIONAL — 2026-09-21, sdd-verificador.** Verde sobre todo lo que
es alcanzable sin mano humana; **la spec NO se cierra** y su estado se deja en
`en-revision` por decisión previa del titular. La transición a `hecho` **no**
se ha registrado.

- **✅ seis CA cerrados**: CA-1, CA-2, CA-3, CA-7, CA-9 y CA-10. Los cuatro
  gates que la casa exige los he corrido yo: `npm run gates` salida 0 (44
  ficheros / 529 tests, build con `ƒ /api/ingest/tick`), `npm run test:db`
  salida 0 (6 ficheros / 63 tests), frontera `src/sources`+`src/decide` vacía y
  cero fugas de los ocho valores de `.env`.
- **⚠️ dos CA parciales**: **CA-4** y **CA-5**. Su artefacto y su test están en
  verde y he contrastado el job contra la base, pero la mitad de *flujo real*
  de ambos (ejecuciones `succeeded` con diez `200` en CA-4; cron activo y 200
  en el panel de Vercel en CA-5) depende de la misma mano humana que CA-6. **El
  encargo daba estos dos por verificables de punta a punta y no lo son**: hoy
  las 26 ejecuciones del job son `failed` y `net._http_response` está vacío.
- **❌ dos CA sin verificar y sin simular**: **CA-6** (sin CLI, token ni
  proyecto de Vercel; comprobado por mí que nada está falseado en el repo y que
  el alias **sí** queda trazado en el build) y **CA-8** (el YAML lo he parseado
  y hace lo que el CA dice, pero GitHub no lo ejecuta desde una rama que no es
  la por defecto).
- **Riesgo declarado de la épica, despejado de primera mano**: pg_cron
  **1.6.4** admite `'30 seconds'` y los **25 intervalos** que medí entre las 26
  ejecuciones caen todos entre **30.013 s y 30.107 s**. El fallback de H-3 no
  hace falta y los números del ledger del implementador cuadran con la base.
- **Ningún test aprobado por formalidad**: los de CA-1, CA-3, CA-7 y sobre todo
  CA-9 afirman la propiedad que el CA pide, no la ausencia de excepción. La
  única excepción está en los tests de CA-8, que son `toContain` sobre el texto
  del YAML (O-1): no invalidan nada, porque CA-8 no exige test, pero no
  cazarían una regresión estructural.
- **Ningún exceso de alcance**: los cuatro cambios que el implementador se dio a
  sí mismo (F-SPEC-008-3, -4, -5 y el `cargar` del workflow) están forzados por
  CA de esta misma spec, son mínimos y no añaden dependencias.

**Lo que queda, y es todo humano**: los pasos 1-6 del handoff. Hasta que el
Vault tenga los dos secretos, CA-4 no puede pasar de ⚠️; hasta que la rama esté
desplegada, CA-5 y CA-6 tampoco; hasta que el workflow esté en `main`, CA-8
tampoco.

## Evidencia visual
<!-- Tabla CA → captura en _qa/SPEC-008/. Informe HTML opcional: _qa/SPEC-008/informe.html -->
n-a: SPEC-008 no toca pantalla. La evidencia es de consola y de base de datos,
y va transcrita en la columna Verif. de cada CA.

## Salvedades / follow-ups
- **F-SPEC-008-1 — CA-6 no se puede hacer desde aquí: necesita mano humana en Vercel.** En este entorno no hay CLI de Vercel (`vercel` no está instalado) ni token (`VERCEL_TOKEN` no está en `.env`) ni proyecto enlazado (no hay `.vercel/`), así que **ni las siete variables de Production ni la ausencia de variables en Preview ni el flujo 401/200/405/503 contra el despliegue se han tocado**. No se ha simulado nada. Lo que hace falta: (i) las siete variables en **Production** (`DATABASE_URL` del pooler transaccional, `API_FOOTBALL_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `INGEST_TICK_TOKEN`, `CRON_SECRET` con el mismo valor que el anterior); (ii) **ninguna** en Preview; (iii) desplegar la rama a producción para que existan `vercel.json` y la ruta con `GET`. Destino: el titular, antes del ensayo del miércoles 2026-09-23.
- **F-SPEC-008-2 — el job de pg_cron está vivo pero sin secretos, y por eso falla cada 30 s.** La migración de CA-4 está aplicada en `dev` y el job dispara puntualmente, pero `vault.decrypted_secrets` está vacío: `url` sale `null` y `net.http_post` revienta antes de encolar nada. Se arregla con **una** orden, `npm run cron:setup`, que exige antes dos líneas nuevas en `.env` del titular: `INGEST_TICK_URL=https://<dominio de producción>/api/ingest/tick` y `CRON_SECRET=<el mismo valor que INGEST_TICK_TOKEN>`. No he inventado la URL de producción a propósito: es dato del titular (la spec se lo asigna) y un valor mal puesto se traga el ensayo. Mientras tanto el job deja ruido en `cron.job_run_details`, que es exactamente lo que `npm run tick:salud` enseña.
- **F-SPEC-008-3 — `vitest.config.mts` gana el alias `server-only` → `tools/empty-module.mjs`.** Es el mismo alias que ya tenía `vitest.db.config.mts` (precedente F-SPEC-006-7) y hacía falta porque CA-1 y CA-2 obligan a importar `src/db/client.ts` y la ruta desde un test, y el paquete `server-only` lanza fuera de un React Server Component. No cambia nada del build: `next build` sigue viendo el `import "server-only"` real.
- **F-SPEC-008-4 — `src/db/schema.db.test.ts` pasa de `import { sql }` a `getSql()`.** Era el único consumidor del `sql` que CA-1 elimina; sin este cambio no compila. Está dentro de CA-1 pero el CA no lo nombra.
- **F-SPEC-008-5 — el workflow de CA-8 gana una entrada `cargar` en `workflow_dispatch`.** La verificación de CA-8 pide «el job `load` lanzado a mano», y con `load` condicionado solo a `push` un `workflow_dispatch` nunca lo ejecutaría. Con `cargar: si` corre solo `load`; con el valor por omisión (`no`) corre solo `sync`, como pide el CA.
- **F-SPEC-008-6 — CA-8 no es verificable hasta que el workflow esté en `main`.** GitHub solo ofrece `workflow_dispatch` (y solo respeta `schedule`) para ficheros presentes en la rama por defecto, así que los tres puntos de verificación de CA-8 —dispatch limpio sin PR, dispatch con cambio forzado que abre PR, y `load` a mano contra `dev`— **no se pueden ejecutar desde la rama**. El YAML sí está validado estructuralmente (parseado con Psych: `on.schedule[0].cron = '0 5 * * 2'`, `on.push.paths`, los dos jobs con sus `if`) y atado con tests de texto. Destino: verificación tras la fusión. **Actualizado tras O-1:** los tests ya no son de texto, sino aserciones sobre el YAML parseado.
- **F-SPEC-008-7 — O-2 era un fallo real mío, y estaba silencioso: arreglado (2026-09-21).** El paso «Sincronizar contra el proveedor» canalizaba `npm run calendario:sync -- "$TEMPORADA" | tee sync-diff.txt` sin `shell: bash`. El shell por omisión de GitHub en Linux es `bash -e {0}`, **sin `pipefail`**, así que el estado del paso era el de `tee` —siempre 0— y no el de `calendario:sync`, que sale 1 cuando el proveedor devuelve `errors`. Modo de fallo: el proveedor cae un martes, el paso «pasa», no hay diff, no se abre PR y la sincronización semanal no hace nada sin que nadie se entere. Arreglado con `shell: bash` (que da `bash --noprofile --norc -eo pipefail {0}`). **Es el único paso del fichero con tubería**: el de «¿Hay diff…?» no tiene ninguna y el de «Abrir o actualizar el PR» ya traía `set -euo pipefail` en su propio cuerpo. Hay test de regresión: «gives every piped step a shell with pipefail (O-2)» recorre los pasos de los dos jobs y exige `shell: bash` o `set -euo pipefail` a todo `run` con tubería. No es cambio de alcance: es un fallo de CA-8 encontrado en verificación.
- **F-SPEC-008-8 — O-1 resuelto sin dependencia nueva (2026-09-21).** Los tests de CA-8 pasan de `toContain` sobre el texto a aserciones sobre el árbol: `on.schedule[0].cron`, `on.push.branches`, `on.push.paths`, los `if` de los dos jobs y cada paso por su `name`. Como el proyecto no tiene parser de YAML y CA-10 prohíbe añadirlo (`node:yaml` no existe en Node 26 y `yaml` solo figura como peer opcional **no instalado** de Vite), `src/arch/deploy.test.ts` lee el subconjunto que usan los workflows —mapas y secuencias por indentación, secuencias de flujo, escalares entrecomillados y bloques `|`— en unas setenta líneas. Tres casos («SPEC-008 CA-8 the reader of the workflow») prueban primero al propio lector con los tres constructos delicados, para que las demás aserciones valgan algo. Comprobado que muerden: sin `shell: bash` cae exactamente el caso de O-2; con `branches: [master]` caen dos. `package-lock.json` intacto.
- **F-SPEC-008-9 — O-4 y O-5 cerrados (2026-09-21).** La fila CA-2 ya no dice que `src/ingest/` solo gane `engine.test.ts`: dice lo que era cierto, que ningún fichero **preexistente** se modificó salvo ese, y nombra los cuatro nuevos que piden CA-3 y CA-7. `src/db/schema.db.test.ts` importa ya `./client.ts` con extensión.
- **F-SPEC-008-10 — CA-3 no se cumplía: `npm run cron:setup` moría contra la base real (2026-09-22, arreglado).** `setupCronSecrets` buscaba los secretos con `where name = any(${sql.array(names)})` y Postgres respondía `op ANY/ALL (array) requires array on right side`, así que **el Vault se quedaba vacío y el job de pg_cron seguía fallando**: justo lo que CA-3 promete que deja resuelto. La causa no es un typo: `sql.array()` resuelve el tipo del array por `options.shared.typeArrayMap`, que postgres.js solo rellena cuando una conexión **termina** de abrirse (`connection.js`, `fetchArrayTypes`, vía `needsTypes`), y el parámetro se construye al montar la plantilla. En la **primera sentencia de un pool nuevo** —que es exactamente lo que hace `cron:setup`— el mapa está vacío, el valor se liga como `text` en vez de `text[]` y Postgres lo rechaza. Arreglado pasando un array JS plano, que se serializa sin ese mapa: corrige la causa, sobrevive en frío, no clava ningún OID a mano y mantiene la forma `any($1)`, así que los ocho casos del doble siguen valiendo.
- **F-SPEC-008-11 — la lección, que importa más que el typo: CA-3 se dio por bueno contra dobles.** Los ocho casos de `cron.test.ts` son verdes y ninguno tocó Postgres; la verificación llegó a ejecutar `cron:setup`, pero desde un cwd sin `.env`, así que abortaba en la comprobación de variables antes de llegar al SQL. **El camino real no corrió hasta que se ejecutó en producción**, con el ensayo a dos días. De ahí `src/ingest/cron.db.test.ts`. Y un matiz medido, no supuesto: **un test en transacción con rollback habría salido verde con el código viejo**, porque `begin` abre la conexión —y calienta el mapa de tipos— antes de que corra el callback. Comprobado revirtiendo el arreglo: de los tres casos nuevos solo cae el frío, con el `PostgresError` exacto de producción. Por eso el caso frío es de **solo lectura**: con los nombres reales en juego, un caso que escribiera pisaría los secretos del titular en cuanto `cron:setup` haya corrido de verdad.
- **F-SPEC-008-12 — el mismo defecto está latente en `src/ingest/engine.ts`, y no lo he tocado.** Las tres apariciones de `sql.array` (`insertDecision`, `resolveAlerts` y las cuatro consultas de `decideMatches`) se salvan hoy solo porque `decideMatches` corre **siempre** dentro de una transacción ya abierta, y para entonces el mapa está caliente; medido: los tres patrones en frío dentro de `sql.begin` funcionan, y el mismo patrón fuera de transacción falla. Era una propiedad de la que depende la corrección del motor y que no estaba escrita en ninguna parte: queda documentada en un comentario sobre `decideMatches`. El cambio de los tres a arrays planos es entrada de EPIC-MANT, no de esta spec.

- **F-SPEC-008-13 — dos pegas de usabilidad de `tick:salud`, encontradas al ejecutar el paso 5 de la puesta en marcha (2026-09-22, arregladas).** No son cambio de alcance de CA-7: los seis bloques, el cierre `REVISAR: …` y la redacción de secretos siguen intactos, y los seis casos originales de `salud.test.ts` pasan sin tocarlos. (i) **La lista de `FALLO` no tenía tope** y llegó a imprimir 116 líneas idénticas, enterrando los intentos, las alertas y los partidos en ventana, que es justo lo que hay que mirar el viernes. Ahora imprime **cinco**, las más recientes, y cierra con `… y M más`: el informe pasó de ~130 líneas a 31 y cabe en una pantalla. (ii) **Un fallo transitorio dejaba el semáforo rojo una hora entera.** Ahora el veredicto lo decide una **ventana corta de 10 minutos** y el resumen de la hora se queda como contexto informativo, de modo que un tick sano ahora se ve verde ahora sin ocultar que hubo problemas antes. Diez minutos son ~20 ejecuciones de un job que dispara cada 30 s —suficiente para que un tick sano se vea— y **menos que los 15 minutos de RN-05**, así que el informe se pone rojo antes de que el motor empiece a abrir alertas de silencio. Medido en `dev`: con el último fallo a las 22:26:32Z, el semáforo se pone verde hacia las 22:36:40Z en vez de a las 23:27Z. `salud.ts` sigue siendo puro: la ventana corta se deriva de `now` con `shiftInstant`, sin tocar el reloj.
- **F-SPEC-008-14 — un hueco del semáforo que NO he tocado, porque contradiría CA-7.** Si el job dejara de dispararse del todo, `tick:salud` saldría **verde**: `ok` solo mira fallos, y cero ejecuciones no es un fallo. CA-7 lo fija así de forma explícita («ninguna fila en ningún bloque → informe completo sin lanzar», con `ok: true`), y el caso está en los seis originales. Pero «pg_cron muerto» es exactamente lo que el semáforo debería cazar el viernes, y hoy no lo caza. Arreglarlo es decidir que sin ejecuciones recientes el informe es rojo, lo que cambia la letra de CA-7: no es mío. Destino: gate humano o EPIC-MANT.


### Observaciones del verificador (2026-09-21)
Ninguna bloquea la spec; las dos primeras van al titular **antes** de la
primera ejecución real del workflow.

- **O-1 — los tests de CA-8 son `toContain` sobre el texto del YAML, no sobre
  su estructura.** `src/arch/deploy.test.ts` afirma cadenas sueltas
  (`"branches: [main]"`, `"workflow_dispatch:"`), que no prueban **dónde**
  están: `branches` podría acabar colgando de `schedule` y el test seguiría en
  verde, y un `workflow_dispatch:` dentro de un comentario también pasaría. He
  parseado el fichero yo con Psych y la estructura **es** correcta hoy, así que
  el artefacto está bien y el CA no exige test alguno; lo que digo es que estos
  tests no cazarían una regresión estructural. Al verificar CA-8 tras la
  fusión, conviene cambiarlos por aserciones sobre el YAML parseado.
- **O-2 — el paso de sincronización se traga un fallo del proveedor.** En
  `calendario-semanal.yml`, el paso «Sincronizar contra el proveedor» es
  `npm run calendario:sync -- "$TEMPORADA" | tee sync-diff.txt` sin declarar
  `shell: bash`. El shell por omisión de GitHub Actions es `bash -e {0}`, **sin
  `pipefail`**, así que el estado de salida del paso es el de `tee` (siempre 0)
  y no el de `calendario:sync`, que sale 1 cuando el proveedor devuelve
  `errors` o la validación falla. Modo de fallo: el proveedor cae un martes →
  el paso «pasa» → no hay diff → no se abre PR → la sincronización semanal no
  hace nada y **nadie se entera**. Arreglo de una línea: `shell: bash` en ese
  paso (o `set -o pipefail`).
- **O-3 — `tickSalud` cuenta como fallo cualquier `status !== 'succeeded'`.**
  Una ejecución en curso (`running`) se lee como `FALLO` y tira el `ok` a
  `false`. Es conservador en la dirección buena (avisa de más, no de menos),
  pero el viernes puede dar un `REVISAR` falso si `npm run tick:salud` cae
  justo mientras un job está en vuelo: conviene saberlo antes de asustarse.
- **O-4 — inexactitud en el ledger del implementador, fila CA-2.** Dice
  «`git diff main -- src/ingest` solo `engine.test.ts`». No es así:
  `src/ingest/` gana cuatro ficheros nuevos (`cron.ts`, `cron.test.ts`,
  `salud.ts`, `salud.test.ts`), que los propios CA-3 y CA-7 exigen. Lo que sí
  es cierto —y es lo que CA-2 quiere decir— es que **ningún fichero
  preexistente** de `src/ingest/` se modifica salvo `engine.test.ts`, que es
  CA-9. La afirmación sustantiva se sostiene; la frase, tal como está escrita,
  no.
- **O-5 — nit heredado.** `src/db/schema.db.test.ts` sigue importando
  `from "./client"` sin `.ts`. Viene de `main` y CA-10 solo exige la extensión
  en los ficheros **nuevos**, así que no es incumplimiento; pero F-SPEC-008-4
  tocó justo esa línea y era gratis dejarla arreglada.

## Cómo retomar (handoff)
**Hecho y verde en local**: CA-1, CA-2, CA-3, CA-4, CA-5, CA-7, CA-9 y CA-10, con `npm run gates` y `npm run test:db` los dos en salida 0. Nueve commits en `ft/SPEC-008-despliegue-del-tick`, uno por CA, sin push ni PR.

**El riesgo declarado de la épica está despejado**: pg_cron **1.6.4** admite `'30 seconds'` y el job dispara cada 30 s exactos contra `dev`. No hace falta el fallback de H-3 ni plantearse los 60 s.

**Lo que queda, y es todo humano**, en este orden y antes del miércoles 2026-09-23:
1. Añadir a `.env` `INGEST_TICK_URL` (URL absoluta de `/api/ingest/tick` en producción) y `CRON_SECRET` (mismo valor que `INGEST_TICK_TOKEN`).
2. Poner las siete variables de CA-6 en **Production** de Vercel y **ninguna** en Preview.
3. Fusionar la rama y desplegar a producción, para que existan `vercel.json`, el cron de respaldo y el `GET` de la ruta.
4. `npm run cron:setup` una vez contra `dev`: deja los dos secretos en Vault y el job deja de fallar.
5. `npm run tick:salud`: debe salir **0** con `succeeded: 100%` y `partidos en ventana: 0`. Ese es el semáforo del ensayo.
6. Verificar CA-6 (401/200/405 contra producción, 503 contra un preview, `ingest_attempts` sin crecer) y CA-8 (`workflow_dispatch` del calendario, ya en `main`).

**Nada de esto es código**: si el paso 5 sale en verde, el tick está desplegado y SPEC-009 hereda una casa lista para medir el viernes.
