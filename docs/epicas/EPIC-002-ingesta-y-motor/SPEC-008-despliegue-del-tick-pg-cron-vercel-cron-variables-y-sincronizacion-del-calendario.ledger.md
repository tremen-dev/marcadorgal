---
id: SPEC-008
tipo: ledger
epica: EPIC-002
---
# Ledger — SPEC-008 Despliegue del tick: pg_cron, Vercel Cron, variables y sincronizacion del calendario

## Resumen
- Fase: **hecho** (GREEN en la 4ª verificación, 2026-09-22: CA-8 ejecutado de punta
  a punta, run `35667936735` y PR #10. CA-5 y CA-6 cierran con salvedad aceptada)
- Rama: `ft/SPEC-008-despliegue-del-tick`, y `ft/SPEC-008-cron-setup-array` para el arreglo de CA-3

## Matriz de criterios de aceptación
<!-- Escritores: sdd-implementador rellena Implementado y Test; sdd-verificador rellena Verif. y Estado. Nunca al revés. -->
<!-- Estados por CA: ✅ cerrado · ⚠️ parcial/con salvedad · 🚧 en curso · ❌ sin empezar · n-a -->
<!-- Un CA está ✅ solo cuando Implementado + Test + Verif. aplicables están en verde. Una salvedad se marca ⚠️, nunca ✅. -->
| CA | Implementado (fichero) | Test (fichero/caso) | Verif. | Estado |
|---|---|---|---|---|
| CA-1 | `src/db/client.ts` (`getSql()` memoizado, `import "server-only"` intacto), `src/app/api/ingest/tick/route.ts` (sin `let pool`, importa `getSql`), `src/db/schema.db.test.ts` (pasa a `getSql()`), `vitest.config.mts` (alias `server-only`, F-SPEC-008-3) | `src/db/client.test.ts` → «SPEC-008 CA-1 src/db/client.ts is lazy» (3 casos: importa sin `DATABASE_URL`, `getSql()` lanza `DATABASE_URL is not set`, dos llamadas `toBe` la misma instancia) | 2026-09-21 sdd-verificador, reproducido: `.env` retirado del árbol + `env -u DATABASE_URL -u API_FOOTBALL_KEY -u NEXT_PUBLIC_SUPABASE_URL -u NEXT_PUBLIC_SUPABASE_ANON_KEY -u SUPABASE_SERVICE_ROLE_KEY -u INGEST_TICK_TOKEN npx next build` → **salida 0** con la ruta `ƒ /api/ingest/tick` (`.env` restaurado, árbol limpio). `grep -c "let pool" src/app/api/ingest/tick/route.ts` → **0**; `src/db/client.ts` conserva `import "server-only"` y exporta solo `getSql`. Los 3 casos de `client.test.ts` son reales: `vi.resetModules()` por caso, `process.env` restaurado en `afterEach`, memoización afirmada con `toBe` y no con `toEqual`.  **2ª vuelta 2026-09-22, sdd-verificador**: repetido con el árbol de `ft/SPEC-008-cron-setup-array`. `.env` retirado del árbol + `env -u` de las ocho variables → `npx next build` **salida 0** con `ƒ /api/ingest/tick` (`.env` restaurado, árbol limpio). `grep -c "let pool" src/app/api/ingest/tick/route.ts` → **0**; `src/db/client.ts` conserva `import "server-only"` y el `pool ??= createSql(process.env)`. | ✅ |
| CA-2 | `src/app/api/ingest/tick/route.ts` → `export const GET = POST`. Ningún fichero **preexistente** de `src/ingest/` modificado salvo `engine.test.ts`, que es CA-9 (`git diff main --name-status --diff-filter=M -- src/ingest`); los cuatro ficheros nuevos de esa carpeta (`cron.ts`, `cron.test.ts`, `salud.ts`, `salud.test.ts`) los piden CA-3 y CA-7 | `src/app/api/ingest/tick/route.test.ts` → «SPEC-008 CA-2 GET and POST on /api/ingest/tick» (3 casos: importa con `process.env = {}`, `GET` `toBe` `POST`, `runtime`/`dynamic`/`maxDuration`) | 2026-09-21 sdd-verificador. Unitario: `GET` `toBe` `POST`, `runtime`/`dynamic`/`maxDuration`, importación con `process.env = {}`. **Flujo real añadido por el verificador** contra el build de producción servido con `next start -p 3187` y `.env`: `POST` sin cabecera → **401**, `GET` sin cabecera → **401**, `POST` con Bearer de igual longitud y distinto valor → **401**, `PUT` con el token bueno → **405**, `POST` con el token bueno → **200** `{"inWindow":0,"purge":"skipped","attempts":[]}`, `GET` con el token bueno → **200** con el mismo cuerpo. H-2 queda probado en ejecución y no solo por identidad de referencia. Ningún fichero **preexistente** de `src/ingest/` modificado salvo `engine.test.ts` (CA-9): `git diff main --name-status --diff-filter=M -- src/ingest` → solo `engine.test.ts`. Lo que falta de CA-2 es únicamente el mismo flujo contra el despliegue de Vercel (CA-6).  **2ª vuelta 2026-09-22, sdd-verificador**: lo que faltaba (el flujo contra el despliegue de Vercel) **ya está hecho**, ver CA-6: `POST`/`GET` sin cabecera → 401, `POST` con Bearer falso → 401, `PUT` con el token bueno → 405, `POST` y `GET` con el token bueno → 200 con el mismo cuerpo, todo contra `https://marcador.gal/api/ingest/tick`. H-2 probado en producción. | ✅ |
| CA-3 | `src/ingest/cron.ts` (`cronSecrets`, `findSecretIds`, `setupCronSecrets`, `TICK_URL_SECRET`, `TICK_TOKEN_SECRET`; la búsqueda pasa a array JS plano el 2026-09-22, F-SPEC-008-10), `tools/cron-setup.mjs`, `package.json` → `cron:setup`, `.env.example` → `INGEST_TICK_URL=` y `CRON_SECRET=` | `src/ingest/cron.test.ts` → 8 casos: Vault vacío → dos `vault.create_secret`; los dos presentes → dos `vault.update_secret`; el informe trae los dos nombres y ninguno de los dos valores; sin `INGEST_TICK_URL` / `INGEST_TICK_TOKEN` lanza nombrándola y sin emitir sentencia; `cron:setup` sale 1 sin variables y sin abrir conexión. **`src/ingest/cron.db.test.ts` (2026-09-22, contra la base real)**: `findSecretIds` como primera sentencia de un pool nuevo (el caso frío, que es el único que caza F-SPEC-008-10); el ciclo entero `creado` → `actualizado` con los valores correctos en `vault.decrypted_secrets`, en transacción con rollback y borrando antes las dos filas dentro de la tx; y ninguna huella del test en el Vault al terminar | 2026-09-21 sdd-verificador. `src/ingest/cron.ts` es puro (recibe `sql` y `env`, no abre nada) y `tools/cron-setup.mjs` es la cáscara. Los 8 casos muerden: el doble de `sql` registra el texto de cada sentencia, así que «dos `create_secret`» / «dos `update_secret`» se afirman por conteo sobre lo emitido, no por ausencia de excepción; «el informe no contiene ningún valor» se afirma con `not.toContain` de los dos valores; «lanza antes de abrir nada» comprueba además `calls` vacío. `cron:setup` se ejecuta de verdad con `spawnSync` en un cwd sin `.env` → salida 1, `stderr` nombra `INGEST_TICK_URL`, `stdout` vacío. `.env.example` gana `INGEST_TICK_URL=` y `CRON_SECRET=` sin valores.  **2ª vuelta 2026-09-22, sdd-verificador — el arreglo del array se juzga de nuevo y el test nuevo SÍ cierra el agujero.** Prueba de mutación hecha por mí: revertido `findSecretIds` a `where name = any(${sql.array(names as string[])})` y corrido `npx vitest run --config vitest.db.config.mts src/ingest/cron.db.test.ts` → **cae exactamente un caso**, «survives being the first statement of a brand new pool», con el `PostgresError: op ANY/ALL (array) requires array on right side` **literal de producción**; los dos casos en transacción pasan igual con el código viejo, que es justo lo que F-SPEC-008-11 afirmaba y ahora está medido por el verificador. `src/ingest/cron.ts` restaurado byte a byte tras la mutación (`diff` contra copia previa, árbol limpio). **El Vault real está lleno**: `select name, created_at from vault.decrypted_secrets` → `ingest_tick_token` e `ingest_tick_url`, creados los dos a las `2026-09-21T22:26:49Z`, y ningún valor consultado ni impreso. `npm run test:db` → **salida 0**, 7 ficheros / 66 tests. | ✅ |
| CA-4 | `supabase/migrations/20260921203746_ingest_tick_job.sql` (única migración nueva): `create extension if not exists supabase_vault`, `unschedule` condicional y `cron.schedule('ingest-tick', '30 seconds', …)` con `net.http_post`, `timeout_milliseconds := 55000`, `body := '{}'::jsonb` y URL/token leídos de `vault.decrypted_secrets` por nombre | `src/db/cron.db.test.ts` → «SPEC-008 CA-4 pg_cron job ingest-tick» (4 casos: imprime `extversion`; `supabase_vault` instalado; una sola fila `ingest-tick`, `active`, `schedule = '30 seconds'`; el `command` menciona `vault.decrypted_secrets` y `net.http_post`, y no contiene ni `Bearer sb` ni el valor de `INGEST_TICK_TOKEN`) | 2026-09-21 sdd-verificador, **contrastado contra `dev` por consulta propia, no contra el ledger**. Artefacto y test: una migración nueva (`git diff main --name-only -- supabase/migrations/` → 1 fichero), `supabase db push --dry-run` → `{"upToDate":true,"migrations":[]}`; `cron.job` tiene **una sola** fila `ingest-tick`, `active = true`, `schedule = '30 seconds'`; su `command` contiene `vault.decrypted_secrets`, `net.http_post` y `timeout_milliseconds := 55000`, y **no** contiene `Bearer sb` (`position` → 0) ni el valor de `INGEST_TICK_TOKEN`. **H-3 confirmado de primera mano**: `pg_cron 1.6.4` (con `pg_net 0.20.4`, `supabase_vault 0.3.1`, PostgreSQL 17.6) aceptó `'30 seconds'`; sobre `cron.job_run_details` conté **26 ejecuciones** entre `20:38:28.639Z` y `20:50:59.155Z` y calculé los **25 intervalos**: todos entre **30.013 s y 30.107 s**. El fallback de H-3 no hizo falta y el objetivo de mediana < 45 s de `vision.md` no depende de él. `select count(*) from ingest_attempts` → **0**. **Salvedad**: la mitad de flujo real del CA («~10 ejecuciones y todas `succeeded`; diez `200` en `net._http_response`») **no se cumple**: las 26 son `failed` con `null value in column "url" of relation "http_request_queue"` y `net._http_response` no tiene **ninguna** fila, porque `vault.decrypted_secrets` está vacío. Es decir: está probado que el job existe y dispara puntualmente cada 30 s, y **no** está probado que el cuerpo del `cron.schedule` llegue a encolar la petición. Se desbloquea con `npm run cron:setup`, que exige `INGEST_TICK_URL` (dato del titular, F-SPEC-008-2) — misma dependencia humana que CA-6.  **2ª vuelta 2026-09-22, sdd-verificador — la salvedad se levanta: la mitad de flujo real se cumple.** Consultado por mí contra `dev`, con la **ventana correcta**, que es lo delicado: `cron.job_run_details` arrastra **217 `failed` legítimos**, todos anteriores a llenar el Vault (el último a las `2026-09-21T22:26:32.902Z`, y los secretos se crearon a las `22:26:49Z`). **Desde ese instante: 59 ejecuciones y las 59 `succeeded`, cero `failed`** (`select status, count(*) … where start_time > '2026-09-21T22:26:40Z'` → solo `succeeded`). `select status_code from net._http_response order by created desc limit 10` → **diez `200`**; la tabla entera son **59 filas y las 59 con `status_code = 200`**, la primera a las `22:27:02.933Z`, o sea la primera ejecución posterior al Vault. `select count(*) from ingest_attempts` → **0** antes y después de todo lo que he hecho hoy (fuera de ventana no hay intento ni petición, RN-08, ADR-008 §4). Artefacto reconfirmado: una sola fila `ingest-tick` en `cron.job`, `active`, `schedule = '30 seconds'`; su `command` contiene `vault.decrypted_secrets`, `net.http_post` y `timeout_milliseconds := 55000` y **no** contiene `Bearer sb` (`position` → 0) ni el valor de `INGEST_TICK_TOKEN` (`position` → 0) ni la URL (`position` → 0). `supabase db push` → `{"upToDate":true,…}`. `pg_cron 1.6.4`, `pg_net 0.20.4`, `supabase_vault 0.3.1`. | ✅ |
| CA-5 | `vercel.json` (solo `crons`, `/api/ingest/tick`, `* * * * *`) | `src/arch/deploy.test.ts` → «SPEC-008 CA-5 vercel.json» + «SPEC-008 CA-5 the cron path carries the alias» (4 casos: un solo cron con esa ruta y horario; `vercel.json` no declara nada más; esa ruta tiene entrada en `outputFileTracingIncludes` con `./data/alias/**/*.json`; la ruta apunta a un `route.ts` que existe) | 2026-09-21 sdd-verificador. `vercel.json` es exactamente `{"crons":[{"path":"/api/ingest/tick","schedule":"* * * * *"}]}` y nada más (`Object.keys` → `["crons"]`). `next.config.ts` tiene `outputFileTracingIncludes["/api/ingest/tick"] = ["./data/alias/**/*.json"]`, comprobado a mano y atado por el test, que además lee la ruta **de `vercel.json`**: si alguien mueve el cron, el test cae de verdad. `CRON_SECRET` no se toca en `src/ingest/auth.ts` (`git diff main -- src/ingest/auth.ts` vacío) y el `GET` con Bearer responde 200 (ver CA-2), que es el camino exacto que usará Vercel Cron. **Salvedad**: la mitad de flujo real del CA (cron activo en el panel de Vercel, última ejecución 200, una invocación por minuto con `inWindow: 0` en los logs) **no se ha tocado**: exige el despliegue, misma dependencia humana que CA-6.  **2ª vuelta 2026-09-22, sdd-verificador — la salvedad SIGUE en pie y no es mía de levantar.** Artefacto reconfirmado: `vercel.json` es exactamente `{"crons":[{"path":"/api/ingest/tick","schedule":"* * * * *"}]}`, `Object.keys` → `["crons"]`; `next.config.ts` mantiene `outputFileTracingIncludes["/api/ingest/tick"]`. El camino que usa Vercel Cron (`GET` con `Authorization: Bearer`) responde **200 en producción**, comprobado por mí. **Lo que NO puedo ver y digo como tal**: que el cron figure activo en el panel de Vercel, su última ejecución, y una invocación por minuto con `inWindow: 0` en los logs de la función. No hay CLI de Vercel en esta máquina, no hay `.vercel/` ni `VERCEL_TOKEN`, y nada en la base distingue una invocación de Vercel Cron de otra: `net._http_response` solo registra lo que sale de pg_net, e `ingest_attempts` no crece fuera de ventana por diseño. Queda a ojo humano en el panel. | ⚠️ |
| CA-6 | Nada que implementar en el repo: son variables del panel de Vercel. **Bloqueado, F-SPEC-008-1** (sin CLI ni token de Vercel en este entorno) | n-a (verificación de flujo real) | 2026-09-21 sdd-verificador: **no verificado, y no simulado**. Comprobado por mí que no hay forma de hacerlo aquí: no existe `.vercel/`, `vercel` no está en el `PATH`, `VERCEL_TOKEN` no está en `.env`, y en el diff de la rama no hay ningún mock, stub ni respuesta de Vercel falseada (la única mención es el texto de F-SPEC-008-1). Tampoco hay ninguna URL de producción incrustada en `src/`, `tools/`, `vercel.json` ni la migración (los `marcador.gal` que aparecen son `User-Agent` preexistentes y valores de test). **Sí verificado, la mitad local del CA**: en mi propio build, `.next/server/app/api/ingest/tick/route.js.nft.json` **contiene** `data/alias/2026-27/api-football.json` (SPEC-006 CA-4, ADR-008 §8). Y el flujo 401/401/401/405/200/200 está probado contra el build de producción en local (CA-2), con `ingest_attempts` en **0** después de las llamadas. Queda literalmente pendiente de mano humana: las siete variables en Production, ninguna en Preview, y el 503 de un preview.  **2ª vuelta 2026-09-22, sdd-verificador — de ❌ a ⚠️: la ruta está desplegada y el flujo contra producción lo he corrido yo.** Contra `https://marcador.gal/api/ingest/tick`: `POST` sin cabecera → **401** `{"error":"unauthorized"}`; `GET` sin cabecera → **401**; `POST` con un Bearer de igual longitud y distinto valor → **401**; `PUT` con el token bueno → **405**; `POST` con el token bueno → **200** `{"now":"2026-09-21T22:50:20.055Z","inWindow":0,"purge":"skipped","attempts":[]}`; `GET` con el token bueno → **200** con el mismo cuerpo. `select count(*) from ingest_attempts` → **0** antes y después de las seis llamadas. Alias trazado en el build: `.next/server/app/api/ingest/tick/route.js.nft.json` contiene `data/alias/2026-27/api-football.json`. Que el 200 salga con `inWindow` calculado prueba de rebote que en **Production** están al menos `DATABASE_URL` e `INGEST_TICK_TOKEN`, y los 59 `200` que pg_net cosecha del mismo endpoint lo confirman de forma sostenida. **Lo que sigue sin verificarse, y lo digo como tal**: (i) el inventario de las **siete** variables en Production y que **ninguna** esté en Preview — es el panel de Vercel y aquí no hay ni CLI ni token; (ii) el **503 de un preview**: los dos despliegues de Preview más recientes (`…-bslc78asr-…` y `…-2hajgx0a2-….vercel.app`) responden **302 Redirecting** con y sin token, porque la Deployment Protection está activa. **No lo he forzado**: está documentado que se desactivó puntualmente para medirlo, y desactivarla otra vez no es trabajo de quien juzga. | ⚠️ |
| CA-7 | `src/ingest/salud.ts` (puro: `tickSalud(input) → { ok, text }`, redacta secretos; **el 2026-09-22 el veredicto pasa a decidirse sobre una ventana corta y la lista de fallos se acota**, F-SPEC-008-13, con `SALUD_RECENT_MINUTES = 10` y `SALUD_FAILURES_SHOWN = 5` en `src/ingest/constants.ts`), `tools/tick-salud.mjs` (cáscara: `cron.job`, `cron.job_run_details` de la última hora, `net._http_response`, `ingest_attempts`, `alerts` sin resolver, `windowMatches`; sale 1 si `!ok`), `package.json` → `tick:salud` | `src/ingest/salud.test.ts` → «SPEC-008 CA-7 tickSalud» (6 casos: hora limpia → `ok: true`, seis bloques y `100%`; ejecución `failed` → `ok: false` nombrando el job; `failed` de hace 90 min → `ok: true`; intento `ok: false` → `ok: false` con su `error`; sin ninguna fila → seis bloques sin lanzar; el token en una fila sale como `[secreto]`). **Tres casos más (2026-09-22)**: hora con fallos viejos pero ventana corta limpia → `ok: true` con la coletilla «N problema(s) en la última hora»; intento fallido anterior a la ventana corta → `ok: true` y aun así visible en su bloque; cuarenta fallos → exactamente cinco líneas `FALLO`, las cinco más recientes, más `… y 35 más`, y el informe entero por debajo de 40 líneas. Comprobado que muerden: con la ventana a 60 min y sin tope caen los tres. **Cuatro casos más (2026-09-22, enmienda N-6)**: job activo y cero ejecuciones en la ventana corta → `ok: false` nombrando la razón, sin un solo fallo en la hora; job con la hora llena pero nada en los últimos 10 min (el último hace 11) → `ok: false`, escrito con 20 ejecuciones reales para que un atajo tipo `runs.length === 0` lo suspenda; job `INACTIVO` y cero ejecuciones → `ok: false`; e invariante `SALUD_RECENT_MINUTES < SILENCE_MINUTES` importando las dos constantes. **13 casos en total, y los tres nuevos vistos en rojo antes del arreglo** | 2026-09-21 sdd-verificador. `src/ingest/salud.ts` es puro de verdad: recibe `now` y las filas, no importa `src/clock.ts` ni `postgres`, no hay `new Date(`. Los 6 casos son los que pide el CA y ninguno es vacío: el de redacción mete el token en `error` **y** en `details` y afirma `not.toContain(TOKEN)` más la presencia de `[secreto]`. **Flujo real ejecutado por el verificador**: `npm run tick:salud` contra `dev` → **salida 1**, cabecera `tick:salud · 2026-09-21T20:53:26.196Z`, los **seis** bloques presentes (`jobs de pg_cron` con `ingest-tick 30 seconds activo`, `ejecuciones (última hora)` con `succeeded: 0%` y 30 líneas `FALLO`, `respuestas de pg_net`, `intentos de ingesta`, `alertas sin resolver`, `partidos en ventana: 0`) y el cierre `REVISAR: el tick no está sano`. Sobre esa salida, `grep -cF` de los **siete** valores de `.env` (incluidos `DATABASE_URL` y `DATABASE_PASSWORD`) → **0** en todos. El semáforo del viernes funciona y hoy está en rojo por la razón correcta.  **2ª vuelta 2026-09-22, sdd-verificador — la enmienda N-6 y los dos arreglos de usabilidad muerden, comprobado por mutación.** `npm run tick:salud` contra `dev` → **salida 0** (era 1): `ejecuciones (últimos 10 min): 20 · succeeded: 100% ← decide el semáforo`, `ejecuciones (última hora): 120 · succeeded: 38%`, **exactamente cinco líneas `FALLO`** y `… y 70 más`, los seis bloques, `partidos en ventana: 0`, cierre `OK` con `(75 problema(s) en la última hora, ninguno en los últimos 10 min)`. El informe entero cabe en una pantalla. Sobre esa salida, `grep -cF` de los **diez** valores de `.env` (los ocho anteriores más `INGEST_TICK_URL` y `CRON_SECRET`) → **0 en los diez**. Los once casos de la letra enmendada están en `salud.test.ts` (13 `it`, los dos extra por F-SPEC-008-15) y **matan a sus mutantes**, medido por mí revirtiendo `src/ingest/salud.ts` una vez por mutante y restaurándolo byte a byte: (M1) quitar `&& !silent` —el código anterior a N-6— → caen **los tres** casos nuevos (5, 6 y 8); (M2) el atajo `runs.length === 0` en vez de `recentRuns.length === 0` → cae **el caso 6** y solo ese, que es exactamente lo que el encargo pedía comprobar; (M3) decidir sobre la hora en vez de la ventana corta → caen los dos casos de F-SPEC-008-13; (M4) quitar el tope de cinco → cae el caso de los cuarenta fallos; (M5) tratar un job `INACTIVO` como base nueva (`jobs.some(active)` en vez de `jobs.length > 0`) → cae **el caso 8**. El caso 7 (`cron.job` vacío → `ok: true`) sobrevive a los cinco, que es la propiedad que la condición de (c) protege. `src/ingest/salud.ts` restaurado idéntico y árbol limpio tras cada mutación. | ✅ |
| CA-8 | `.github/workflows/calendario-semanal.yml`: **(2026-09-22, 2ª vuelta)** `fetch-depth: 0` en el checkout del job `sync` y el paso del PR **construye encima de la rama remota** en vez de reescribirla, así que el push es avance rápido y desaparece `--force-with-lease` (F-SPEC-008-19). `src/calendar/sync.ts` ordena los partidos por `round + home + away` y ya no por `kickoff`, y compara texto por code point y no con `localeCompare` (F-SPEC-008-20). `data/calendario/2026-27/*.json` normalizado al orden canónico en un commit aparte. `schedule: '0 5 * * 2'` + `workflow_dispatch`; job `sync` (npm run calendario:sync, PR `chore/calendario-<fecha>` solo con diff, cuerpo con la salida de `formatSyncDiff`); job `load` en `push` a `main` con `paths: [data/calendario/**, data/alias/**]` (entrada `cargar` para lanzarlo a mano, F-SPEC-008-5) | `src/arch/deploy.test.ts` → «SPEC-008 CA-8 calendario-semanal.yml» (4 casos: martes 05:00Z y `workflow_dispatch`; sync + PR solo con diff en `data/calendario`/`data/alias`; load en push a `main` con los dos `paths` y `secrets.DATABASE_URL`; `secrets.API_FOOTBALL_KEY` y nunca la clave en el repo). **Cuatro casos más (2026-09-22)**: `src/arch/deploy.test.ts` → el paso del PR no lleva `--force` en ninguna orden (comentarios aparte), trae `git fetch` de la rama remota, `git ls-remote` para los dos casos y un `git push` de avance rápido; y el checkout del job que empuja lleva `fetch-depth: 0`. `src/calendar/sync.test.ts` → «(O-6) a reschedule rewrites the kickoff in place and moves nothing»: mismo orden de partidos antes y después, exactamente un campo distinto. Comprobado que muerden: con el push viejo cae el primero, sin `fetch-depth` el segundo, y con el orden por `kickoff` el tercero | 2026-09-21 sdd-verificador: **verificado como artefacto, no en ejecución** (F-SPEC-008-6 es correcto: GitHub solo ofrece `workflow_dispatch` y respeta `schedule` para ficheros de la rama por defecto). Parseado por mí con Psych, no por `toContain`: `on.schedule[0].cron = '0 5 * * 2'`; `on.workflow_dispatch.inputs` = `temporada` (`2026-27`) y `cargar` (`no`); `on.push.branches = ['main']` y `on.push.paths = ['data/calendario/**','data/alias/**']`; job `sync` con `if: github.event_name != 'push' && inputs.cargar != 'si'` (6 pasos) y job `load` con `if: github.event_name == 'push' || inputs.cargar == 'si'` (4 pasos) — las dos expresiones son correctas para los tres disparadores. El cuerpo del PR lleva la salida de `formatSyncDiff`: comprobado que `tools/calendario-sync.mjs:112` hace `console.log(formatSyncDiff(...))` y el paso la captura con `tee sync-diff.txt`. `API_FOOTBALL_KEY` y `DATABASE_URL` vienen de `secrets`, nunca del repo. **Dos observaciones, O-1 y O-2 abajo.** Queda pendiente de mano humana, tras la fusión: dispatch limpio sin PR, dispatch con cambio forzado que abre PR, y `load` a mano contra `dev`.  **2ª vuelta 2026-09-22, sdd-verificador — EJECUTADO DE VERDAD CONTRA `main`, Y FALLA. Este es el motivo del RED.** El YAML está en `main` desde el PR #8 y `gh workflow list` lo da `active`, así que por fin es verificable en ejecución y no solo como artefacto. Los tres puntos de verificación del CA: **(i) dispatch limpio sin PR → NO OBSERVABLE hoy**: lancé `gh workflow run calendario-semanal.yml --ref main` (run `35664756167`) y el proveedor **sí traía cambios** (un aplazamiento real: `levante`–`athletic-club` de `2026-09-16T19:30Z` a `2026-10-21T18:00Z`), así que el caso «sin diff» no se dio. **No he forzado ningún cambio en `data/calendario/`** para fabricarlo. **(ii) dispatch con diff → abre PR: FALLA.** El paso «Abrir o actualizar el PR» commitea y hace push de `chore/calendario-2026-09-21` (5 ficheros, 619+/619−) y luego muere: `pull request create failed: GraphQL: GitHub Actions is not permitted to create or approve pull requests (createPullRequest)`, **salida 1**. Causa confirmada por mí vía API: `gh api repos/tremen-dev/marcadorgal/actions/permissions/workflow` → `{"default_workflow_permissions":"read","can_approve_pull_request_reviews":false}`. El `permissions: pull-requests: write` del workflow no puede saltarse ese ajuste de repo/organización. Resultado real: **rama empujada al remoto y ningún PR**, que es el peor de los dos mundos. **(iii) `load` a mano contra `dev` → FALLA.** `gh workflow run … -f cargar=si` (run `35664983849`): el job `load` corre, y el paso «Cargar el calendario en la base» sale **1** con `DATABASE_URL is not set`; en el log del paso se ve `DATABASE_URL:` **vacío**. Causa confirmada: `gh api repos/tremen-dev/marcadorgal/actions/secrets` → **el único secreto del repo es `API_FOOTBALL_KEY`**; `DATABASE_URL` no existe. `select count(*) from calendar_loads` → **5 antes y 5 después**: ninguna fila nueva, que es justo lo que el CA exige. Y no es solo el dispatch: el mismo job es el que dispara `on: push` a `main` con `paths: [data/calendario/**, data/alias/**]`, así que **hoy toda fusión del PR del calendario dejaría la base sin cargar**. El artefacto sigue siendo correcto (lo reverifiqué parseado); lo que no funciona es el workflow desplegado.  **3ª vuelta 2026-09-22, sdd-verificador — las dos causas de configuración están fuera, el código nuevo lo he juzgado a fondo, y aun así CA-8 NO se ha ejecutado nunca entero.** **Causas anteriores, cerradas y comprobadas por mí vía API**: `gh api repos/tremen-dev/marcadorgal/actions/permissions/workflow` → `can_approve_pull_request_reviews: **true**` (F-SPEC-008-16 fuera), y `gh api …/actions/secrets` → `API_FOOTBALL_KEY` **y `DATABASE_URL`** (F-SPEC-008-17 fuera). **El arreglo del push (F-SPEC-008-19), simulado por mí con un `origin` de mentira y el guion del paso copiado literal del YAML, en cinco escenarios**: (A) la rama existe con un commit humano encima → push de **avance rápido** (`8e6bfc9..6ceb5be`), el commit del humano **sigue siendo ancestro** y su fichero fuera de `data/` sobrevive; (B) la rama no existe → `* [new branch]`, creada desde `main`; (C) **alguien empuja entre el `fetch` y el `push`** → el push se **rechaza** con código **1** (no fast-forward) y el empujón ajeno sobrevive intacto: el job falla, que es lo que debe pasar; (D) la rama ya trae ese mismo calendario → **no se añade commit** y se pasa a actualizar el cuerpo del PR; (E) un humano editó `data/calendario/` **dentro** de la rama → ese contenido **se revierte** en el commit nuevo (el commit sigue en la historia). O-8 abajo sobre (E). Conclusión: **no reescribe la rama y falla cuando debe**. **Los tres guardas nuevos muerden**, medido revirtiendo el código y restaurándolo byte a byte: devolver `git push --force-with-lease` → cae «pushes without force…»; quitar `fetch-depth: 0` → cae «checks out the whole history…»; volver a ordenar por `kickoff` → caen **dos** casos de `sync.test.ts`, el de O-6 y el del borrador inicial. **`calendario:sync` contra el proveedor real, corrido por mí**: salida **0** y `git diff --stat -- data/` → **`1 file changed, 1 insertion(+), 1 deletion(-)`**, y esa línea es el aplazamiento (`"kickoff": "2026-09-16T19:30:00Z"` → `"2026-10-21T18:00:00Z"`), con el informe nombrándolo (`~ primera-division-2026-27-j6-levante-athletic-club`). O-6 cerrado de verdad: de 619+/619− a una línea. **Segunda pasada seguida**: `rescheduled: 0` en las cinco competiciones y el árbol no se mueve, o sea la rama `cambios=no` del paso «¿Hay diff…?» — que es F-SPEC-008-18 observado sobre datos reales sin fabricar nada. Árbol restaurado limpio las dos veces. **`calendario:load -- 2026-27` contra `dev`, corrido por mí dos veces**: salida **0** las dos, **0 insertados, 0 actualizados, 1834 sin cambios, 0 huérfanos** en las cinco competiciones y 98 alias; `calendar_loads` pasa de **5 a 9** filas. Idempotente y con fila nueva, que es lo que pide el CA. **LO QUE FALTA, Y ES EL MOTIVO DEL RED (F-SPEC-008-22)**: el workflow **nunca se ha ejecutado con este YAML**. `ft/SPEC-008-cron-setup-array` **no está en `origin`** (`git ls-remote --heads` no la lista), así que no hay ref contra el que lanzar el dispatch, y empujarla no es trabajo de quien juzga; además mi propio `gh workflow run` fue **denegado por el clasificador de permisos** de este entorno. Resultado: **este workflow no ha abierto un PR ni una sola vez**. Las dos veces anteriores el artefacto también parecía correcto y falló en ejecución por dos motivos distintos, así que no lo doy por bueno sobre simulación. Lo que lo cierra: empujar la rama y `gh workflow run calendario-semanal.yml --ref ft/SPEC-008-cron-setup-array`, o fusionar y lanzarlo contra `main`.  **4ª vuelta 2026-09-22, sdd-verificador — EJECUTADO DE PUNTA A PUNTA. ✅.** Contrastado por mí, no aceptado de palabra. `gh run view 35667936735` → job `sync` **✓ success** sobre `ft/SPEC-008-cron-setup-array`: **la primera vez que este workflow termina bien** (el job `load` sale sin correr, que es lo correcto con `cargar` en su valor por omisión). **PR #10 abierto**, `chore/calendario-2026-09-21` → `main`, 5 ficheros, `MERGEABLE`, con el título del CA. **El cuerpo lleva la salida de `formatSyncDiff`** y nombra el aplazamiento en una línea: `~ primera-division-2026-27-j6-levante-athletic-club: 2026-09-16T19:30:00Z -> 2026-10-21T18:00:00Z`, con `rescheduled: 0` en las otras cuatro competiciones. **El push fue avance rápido, comprobado por mí**: `git merge-base --is-ancestor 91364fd origin/chore/calendario-2026-09-21` → **verdadero**, y `git log` de la rama muestra `03126ba` **encima** de `91364fd`, el commit que dejó el run fallido de la 2ª vuelta. La rama **no se reescribió**: es exactamente la propiedad que el `--force-with-lease` no daba y que F-SPEC-008-19 prometía. **El PR sale +1463/−1463 y lo he auditado, porque era el único punto dudoso.** Dos comprobaciones independientes: **(a)** `git diff --stat HEAD origin/chore/calendario-2026-09-21 -- data/` —es decir, el PR contra la rama de spec, que es la **misma base normalizada**— da **`1 file changed, 1 insertion(+), 1 deletion(-)`**, y esa línea es el aplazamiento. **La propiedad de «una línea» de O-6 SÍ está en el PR que produjo el workflow**; lo que la tapa es que la rama `chore/…` cuelga del `main` sin normalizar, no el sync. **(b)** Comparación estructural `main` contra el head del PR como multiconjunto con serialización canónica: en los cinco ficheros la **única** diferencia de dato en todo el calendario es ese partido y su `kickoff`; los 98 equipos y los otros 1833 partidos son idénticos. Los 1462 restantes son **solo reordenación**. **No hay ningún cambio de dato escondido en el PR.** **Juicio**: CA-8 exige que el workflow sincronice y, habiendo diff, abra el PR con el diff y la salida de `formatSyncDiff`. Eso está **ejecutado y verificado**. El tamaño del diff es O-6, que medí cerrado contra el proveedor real, y aquí está explicado y auditado hasta el último elemento; además el cuerpo del PR nombra el aplazamiento, así que el humano de D-3 **no** se queda ciego ni con 1463 líneas delante. **F-SPEC-008-22 cerrado.** | ✅ |
| CA-9 | Sin código nuevo: `createEngineSweep` ya existía (SPEC-007) | `src/ingest/engine.test.ts` → «SPEC-008 CA-9 createEngineSweep» (4 casos: abre exactamente una `db.transaction` y antes de ella no hay sentencia; las cuatro consultas de SPEC-007 CA-9 una sola vez y con los dos `matchId`; `counts.matches === 2`; con ventana vacía no emite ninguna sentencia) | 2026-09-21 sdd-verificador: **el test afirma la propiedad, no la ausencia de excepción**, que era justo el riesgo. `fakeDb` hace `transactions.push(calls.length)` en cada `db.transaction`, y el caso afirma `expect(transactions).toEqual([0])`: eso es **exactamente una** transacción (longitud 1) y abierta **antes** de cualquier sentencia (posición 0) — una segunda transacción daría `[0, n]` y caería. El segundo caso afirma `calls` de longitud **4** y que las cuatro consultas de SPEC-007 CA-9 (`from matches`, `from decisions`, el `last heard` y el `from observations`) llevan cada una los **dos** `matchId` ordenados; el tercero, `counts.matches === 2`. O-1 de SPEC-007 queda cerrado de verdad. El cuarto caso documenta con honestidad que con ventana vacía la transacción se abre igual y no se emite nada (la guarda de ADR-009 §1 vive en `runTick`).  **2ª vuelta 2026-09-22, sdd-verificador**: sin cambios en la rama (`git diff main -- src/ingest/engine.test.ts` vacío) y los cuatro casos siguen en verde dentro de los gates. Se mantiene ✅. | ✅ |
| CA-10 | `package.json` sin dependencias nuevas y solo dos scripts (`cron:setup`, `tick:salud`); una sola migración nueva; imports relativos con `.ts` en los ficheros nuevos | Cubierto por los gates y los greps de frontera (ver abajo) | 2026-09-21 sdd-verificador, **todo corrido por mí**. `env -u DATABASE_URL -u API_FOOTBALL_KEY -u NEXT_PUBLIC_SUPABASE_URL -u SUPABASE_SERVICE_ROLE_KEY -u INGEST_TICK_TOKEN npm run gates` → **salida 0**: Vitest **44 ficheros / 529 tests** en verde, `next build` con `ƒ /api/ingest/tick`. `npm run test:db` → **salida 0**: `db:push` `{"upToDate":true}` y Vitest **6 ficheros / 63 tests**. `git diff main --stat -- src/sources src/decide` → **vacío**. `grep -rnE "new Date\(|Date\.now\(" src/ingest src/raw src/decide` → **vacío**. Fuga de secretos: bucle `git grep -cF` **y** `grep -rlF` sobre el árbol con los **ocho** valores de `.env` (`API_FOOTBALL_KEY`, `DATABASE_PASSWORD`, `DATABASE_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `INGEST_TICK_TOKEN`, `SUPABASE_ACCESS_TOKEN`) → **0 coincidencias** en los dos, con `vercel.json` y la migración en el árbol. Ninguna URL de producción incrustada. `git diff main --stat -- package-lock.json` → **vacío** y el diff de `package.json` son **solo** las dos líneas `cron:setup` y `tick:salud`. `supabase/migrations/`: **un** fichero nuevo. Ningún import relativo sin `.ts` en los ficheros nuevos de la rama (barrido automático). Árbol limpio al terminar.  **2ª vuelta 2026-09-22, sdd-verificador, todo corrido por mí sobre `ft/SPEC-008-cron-setup-array`.** `npm run gates` → **salida 0**: Vitest **44 ficheros / 541 tests** en verde (12 más que en la 1ª vuelta: los 3 de `cron.db.test.ts` —que van por `test:db`— no cuentan aquí; los 12 son los 7 nuevos de `salud.test.ts` y el resto del arrastre), `next build` con `ƒ /api/ingest/tick`. `npm run test:db` → **salida 0**: `db:push` `{"upToDate":true,…}`, Vitest **7 ficheros / 66 tests**. **Frontera**: `git diff main --stat -- src/sources src/decide` → **vacío**; `git diff main -- src/ingest/engine.ts` → **solo el comentario** de F-SPEC-008-12 (12 líneas, todas `//`, sobre `decideMatches`) y **ni una línea de lógica**, verificado leyendo el diff entero. `grep -rnE "new Date\(|Date\.now\(" src/ingest src/raw src/decide` → **vacío**. **Fugas**: bucle `grep -cF` sobre la salida de `tick:salud` **y** `git grep -cF` **y** `grep -rlF` sobre el árbol (sin `node_modules`/`.git`/`.next`/`.env`) con los **diez** valores de `.env` → **0 en todos**, con una única aparición esperable y **no secreta**: la URL pública `https://marcador.gal/api/ingest/tick` figura como literal de test en `src/ingest/cron.test.ts:15` y `src/ingest/handler.test.ts:14` (el segundo viene de `main`). `package.json` y `package-lock.json` sin diff en la rama; los scripts del proyecto son solo `cron:setup` y `tick:salud` de nuevos y no hay dependencias nuevas. Una sola migración de esta spec en `supabase/migrations/` (`20260921203746_ingest_tick_job.sql`), **sin ningún secreto** dentro (`grep -cF` del token → 0). Imports relativos con `.ts` en todos los ficheros nuevos de la rama (barrido automático). Árbol limpio al terminar salvo este ledger.  **3ª vuelta 2026-09-22**: `npm run gates` → **salida 0** (44 ficheros / **544 tests**, build con `ƒ /api/ingest/tick`); `npm run test:db` → **salida 0** (7 ficheros / 66 tests); `npm run tick:salud` → **salida 0**. Frontera intacta: `git diff main --stat -- src/sources src/decide` **vacío** y `src/ingest/engine.ts` **solo comentario**. `package.json` y `package-lock.json` sin diff. Cero fugas de los diez valores de `.env`. | ✅ |
### Evidencia de ejecución (sdd-implementador, 2026-09-21)
- **Gates, sin `.env` en el árbol y con el entorno desnudo** (`mv .env` fuera + `env -u DATABASE_URL -u API_FOOTBALL_KEY -u NEXT_PUBLIC_SUPABASE_URL -u SUPABASE_SERVICE_ROLE_KEY -u INGEST_TICK_TOKEN npm run gates`): **salida 0**. `vitest run` → **44 ficheros, 529 tests, todo verde**; `next build` → `ƒ /api/ingest/tick` (CA-1).
- **`npm ci` en el mismo entorno desnudo**: salida 0.
- **`npm run test:db`**: **salida 0**. `db:push` → `{"upToDate":true,...}`; `vitest --config vitest.db.config.mts` → **6 ficheros, 63 tests** en verde.
- **`supabase db push --dry-run --db-url "$DATABASE_URL"`** → `{"upToDate":true,"dryRun":true,"migrations":[]}`.
- **pg_cron (H-3): `extversion = 1.6.4`, y aceptó `'30 seconds'` sin discusión.** El fallback de H-3 (job por minuto con dos `net.http_post` y un `pg_sleep(30)`) **no ha hecho falta**. Medido en `cron.job_run_details`: 15 ejecuciones entre `20:38:28.639Z` y `20:45:28.965Z`, separadas exactamente 30 s.
- **pg_net y Vault**: `pg_net 0.20.4`, `supabase_vault 0.3.1`, PostgreSQL 17.6. `net.http_post` vive en el esquema `net` y `net._http_response` también, como dan por hecho CA-4 y CA-7.
- **Estado del job hoy**: programado y disparando cada 30 s, pero las 15 ejecuciones son `failed` con `null value in column "url" of relation "http_request_queue"`, porque el Vault está vacío: falta `npm run cron:setup`, que a su vez necesita `INGEST_TICK_URL` en `.env` (F-SPEC-008-2). `select count(*) from ingest_attempts` → **0** (ninguna petición ha salido y estamos fuera de ventana).
- **Alias trazado en el build** (mitad local de CA-6): `.next/server/app/api/ingest/tick/route.js.nft.json` contiene `data/alias/2026-27/api-football.json`.
- **Frontera (CA-10)**: `git diff main --stat -- src/sources src/decide` → **vacío**. `grep -rnE "new Date\(|Date\.now\(" src/ingest src/raw src/decide` → **vacío**. `git grep -F` de `INGEST_TICK_TOKEN`, `API_FOOTBALL_KEY`, `SUPABASE_SERVICE_ROLE_KEY` y `DATABASE_PASSWORD` → **sin coincidencias** (con `vercel.json` y la migración en el árbol).
- **`npm run tick:salud` contra `dev`**: imprime los seis bloques, marca las cinco ejecuciones `failed` de la última hora y sale **1** con `REVISAR: el tick no está sano`. Es el comportamiento correcto: el job existe y falla porque le falta el secreto.

## Veredicto del verificador
<!-- GREEN/RED + fecha + resumen. Lo escribe SOLO sdd-verificador. -->
**GREEN — 2026-09-22, sdd-verificador (4ª y última verificación).** Los diez CA
quedan cerrados: **ocho ✅** y **dos ⚠️ con salvedad justificada y aceptada por
el titular** (CA-5 y CA-6, ver abajo). **Registro la transición a `hecho`.**

- **CA-8 cumplido, y por fin en ejecución.** Run `35667936735` **success**, PR
  **#10** abierto con el cuerpo de `formatSyncDiff` nombrando el aplazamiento, y
  push de **avance rápido** con `91364fd` todavía en la historia. Es la primera
  vez que este workflow termina bien, y las tres propiedades que F-SPEC-008-19
  prometía se ven en el remoto, no en una simulación.
- **El +1463/−1463 del PR no es un problema, y lo he auditado hasta el fondo**:
  contra la misma base normalizada el PR es **una línea**, la del aplazamiento; y
  la comparación estructural `main` ↔ PR como multiconjunto da **un solo dato
  distinto en todo el calendario**, ese mismo `kickoff`. Lo demás es
  reordenación. Ningún cambio de dato escondido.
- **La normalización, limpia por tres caminos** (multiconjunto canónico,
  `calendario:load` con 0/0/1834, y el diff literal contra la rama de spec).
- **Gates en la 3ª vuelta, corridos por mí**: `npm run gates` **salida 0** (44
  ficheros / 544 tests), `npm run test:db` **salida 0** (7 / 66),
  `npm run tick:salud` **salida 0**. Frontera intacta y cero fugas.
- **Las dos salvedades que se cierran con ⚠️, no con ✅**, y que **no** son
  incumplimiento: **CA-5** (cron activo y logs en el panel de Vercel) y **CA-6
  (i) y (ii)** (inventario de las siete variables de Production / ninguna en
  Preview, y el 503 de un preview, hoy 302 por Deployment Protection). Las dos
  son estructuralmente inalcanzables desde aquí, están documentadas desde la 1ª
  vuelta y el titular las acepta. La mitad ejecutable de CA-6 la corrí yo:
  401/401/401/405/200/200 contra producción con `ingest_attempts` en 0.

---

**RED — 2026-09-22, sdd-verificador (3ª verificación, acotada a CA-8 y a la
normalización).** Un RED mucho más estrecho que el anterior: **nueve CA en
verde**, los dos bloqueantes de la 2ª vuelta cerrados y comprobados por mí, la
normalización limpia por tres caminos independientes, y O-6 resuelto de verdad.
**CA-8 sigue sin cumplirse por una sola razón: nunca se ha ejecutado.**

- **La normalización es limpia, comprobado por mí y por camino propio.**
  Serialización canónica con claves ordenadas recursivamente y comparación como
  multiconjunto: en los cinco ficheros, `competition` idéntico, **98** equipos y
  **1834** partidos con multiconjunto **idéntico**, cero elementos solo en un
  lado, cero duplicados. Y por un tercer camino, `calendario:load` contra `dev`:
  **0 insertados, 0 actualizados, 1834 sin cambios**. **Ni un dato cambia.**
  (El «1760» del ledger es incorrecto: son 1834. F-SPEC-008-21.)
- **O-6 cerrado con el proveedor real**: `calendario:sync` → salida 0 y
  **`1 file changed, 1 insertion(+), 1 deletion(-)`**, y esa línea es el
  aplazamiento. De 619+/619− a una. Segunda pasada seguida: `rescheduled: 0` en
  las cinco y el árbol quieto — la rama `cambios=no` de F-SPEC-008-18, observada
  sobre datos reales y sin fabricar nada.
- **El arreglo del push es bueno**, y lo he probado, no leído: avance rápido
  sobre la rama remota, la historia ajena intacta, **rechazo con código 1** si
  alguien empuja en medio, y funciona exista la rama o no. Los tres guardas
  nuevos mueren cada uno en su mutante.
- **Las dos causas de la 2ª vuelta, fuera**: `can_approve_pull_request_reviews:
  true` y el secreto `DATABASE_URL` presente, las dos comprobadas por API. El
  `load` corrido por mí: salida 0, idempotente, `calendar_loads` de 5 a 9.
- **Y aun así, ❌ CA-8**: `ft/SPEC-008-cron-setup-array` **no está en `origin`**,
  así que no hay ref contra el que lanzar el dispatch, y mi `gh workflow run` fue
  además **denegado por el clasificador de permisos**. **Este workflow no ha
  abierto un PR ni una sola vez.** Las dos veces anteriores el artefacto también
  parecía correcto y falló en ejecución por dos motivos distintos; sobre
  simulación no lo firmo. Es la verificación que el propio CA pide.
- **Gates corridos por mí**: `npm run gates` **salida 0** (44 ficheros / **544
  tests**), `npm run test:db` **salida 0** (7 / 66), `npm run tick:salud`
  **salida 0** (119/119 `succeeded` en la hora; el cambio al pooler 6543 en
  Vercel no ha roto nada: 20/20 y veinte `200` en los últimos diez minutos).
- **CA-5 y CA-6 siguen ⚠️** por el panel de Vercel y el 503 del preview, como
  estaba acordado, y **no cuentan como incumplimiento**.

**Un solo paso para GREEN**: empujar la rama y lanzar el `workflow_dispatch`
contra ella (o fusionar y lanzarlo contra `main`). Con ver el PR abierto y su
diff de una línea, CA-8 cierra y los diez CA quedan en verde.

---

**RED — 2026-09-22, sdd-verificador (2ª verificación, final).** La puesta en
marcha ha desbloqueado casi todo lo que la 1ª vuelta no pudo tocar, y he
levantado cuatro CA con evidencia propia; pero **CA-8 no se cumple**, y esta vez
no por falta de acceso sino porque lo he ejecutado y falla. La spec **no** pasa a
`hecho`; el estado se queda en `en-progreso` y no lo he tocado.

- **✅ siete CA cerrados**: CA-1, CA-2, CA-3, CA-4, CA-7, CA-9 y CA-10. Los que
  cambian respecto a la 1ª vuelta son **CA-4** (de ⚠️ a ✅: 59 ejecuciones
  `succeeded` y 59 respuestas `200` desde que el Vault se llenó) y **CA-3** y
  **CA-7**, que ya estaban en ✅ pero ahora lo están por mérito propio del
  código nuevo y no por herencia.
- **⚠️ dos CA parciales**: **CA-5** y **CA-6**. Lo que falta de los dos es
  exactamente lo que no se puede ver sin el panel de Vercel (el cron activo y
  sus logs; el inventario de las siete variables de Production y la ausencia en
  Preview) más el **503 de un preview**, que hoy da **302** porque la Deployment
  Protection está activa. No lo he forzado.
- **❌ un CA incumplido, y es el RED: CA-8.** El workflow está en `main` y se
  deja lanzar, así que por fin es verificable en ejecución. Lo lancé dos veces y
  **las dos fallaron**: el `sync` empuja la rama del calendario y muere sin abrir
  el PR (`GitHub Actions is not permitted to create or approve pull requests`,
  F-SPEC-008-16), y el `load` muere con `DATABASE_URL is not set` porque ese
  secreto **no existe** en el repositorio (F-SPEC-008-17). El segundo no es solo
  un hueco de verificación: es el job que corre en cada `push` a `main` que toca
  `data/calendario/**`, así que hoy el calendario fusionado **no entra en base**.
- **Gates corridos por mí, con salida real**: `npm run gates` → **salida 0**
  (44 ficheros / 541 tests, build con `ƒ /api/ingest/tick`); `npm run test:db` →
  **salida 0** (7 ficheros / 66 tests); `npm run tick:salud` → **salida 0**, que
  era el semáforo del ensayo y hoy está verde.
- **Los tres arreglos nuevos los he juzgado por mutación, no por lectura.** El de
  `cron.ts` y los cinco mutantes de `salud.ts` mueren cada uno en el caso que le
  toca (detalle en las filas CA-3 y CA-7). Ningún test nuevo pasa por formalidad:
  revertido el código, el test correspondiente se pone rojo. En particular el
  caso 6 de CA-7 suspende el atajo `runs.length === 0`, y el caso 7 (`cron.job`
  vacío → `ok: true`) sobrevive a los cinco mutantes.
- **Frontera respetada**: `git diff main --stat -- src/sources src/decide`
  **vacío**, y el único cambio en `src/ingest/engine.ts` son **12 líneas de
  comentario** sobre `decideMatches` (F-SPEC-008-12), sin una sola línea de
  lógica. El defecto latente de `sql.array` sigue sin tocar, como corresponde:
  es EPIC-MANT.
- **Sin fugas**: los **diez** valores de `.env` dan 0 coincidencias en el árbol,
  en `git grep` y en la salida de `tick:salud`.

**Lo que hay que arreglar para cerrar (los dos son de configuración, no de
código)**: F-SPEC-008-16 y F-SPEC-008-17. Con eso hecho, relanzar el
`workflow_dispatch` en sus dos modos y CA-8 se cierra sin tocar el YAML.

---

**GREEN CONDICIONAL — 2026-09-21, sdd-verificador.** Verde sobre todo lo que
es alcanzable sin mano humana; **la spec NO se cierra** y su estado se deja en
`en-revision` por decisión previa del titular. La transición a `hecho` **no**
se ha registrado.

- **✅ seis CA cerrados**: CA-1, CA-2, CA-3, CA-7, CA-9 y CA-10. Los cuatro
  gates que la casa exige los he corrido yo: `npm run gates` salida 0 (44
  ficheros / 529 tests, build con `ƒ /api/ingest/tick`), `npm run test:db`
  salida 0 (6 ficheros / 63 tests), frontera `src/sources`+`src/decide` vacía y
  cero fugas de los ocho valores de `.env`.
- **⚠️ dos CA parciales**: **CA-4** y **CA-5**. Su artefacto y su test están en
  verde y he contrastado el job contra la base, pero la mitad de *flujo real*
  de ambos (ejecuciones `succeeded` con diez `200` en CA-4; cron activo y 200
  en el panel de Vercel en CA-5) depende de la misma mano humana que CA-6. **El
  encargo daba estos dos por verificables de punta a punta y no lo son**: hoy
  las 26 ejecuciones del job son `failed` y `net._http_response` está vacío.
- **❌ dos CA sin verificar y sin simular**: **CA-6** (sin CLI, token ni
  proyecto de Vercel; comprobado por mí que nada está falseado en el repo y que
  el alias **sí** queda trazado en el build) y **CA-8** (el YAML lo he parseado
  y hace lo que el CA dice, pero GitHub no lo ejecuta desde una rama que no es
  la por defecto).
- **Riesgo declarado de la épica, despejado de primera mano**: pg_cron
  **1.6.4** admite `'30 seconds'` y los **25 intervalos** que medí entre las 26
  ejecuciones caen todos entre **30.013 s y 30.107 s**. El fallback de H-3 no
  hace falta y los números del ledger del implementador cuadran con la base.
- **Ningún test aprobado por formalidad**: los de CA-1, CA-3, CA-7 y sobre todo
  CA-9 afirman la propiedad que el CA pide, no la ausencia de excepción. La
  única excepción está en los tests de CA-8, que son `toContain` sobre el texto
  del YAML (O-1): no invalidan nada, porque CA-8 no exige test, pero no
  cazarían una regresión estructural.
- **Ningún exceso de alcance**: los cuatro cambios que el implementador se dio a
  sí mismo (F-SPEC-008-3, -4, -5 y el `cargar` del workflow) están forzados por
  CA de esta misma spec, son mínimos y no añaden dependencias.

**Lo que queda, y es todo humano**: los pasos 1-6 del handoff. Hasta que el
Vault tenga los dos secretos, CA-4 no puede pasar de ⚠️; hasta que la rama esté
desplegada, CA-5 y CA-6 tampoco; hasta que el workflow esté en `main`, CA-8
tampoco.

## Evidencia visual
<!-- Tabla CA → captura en _qa/SPEC-008/. Informe HTML opcional: _qa/SPEC-008/informe.html -->
n-a: SPEC-008 no toca pantalla. La evidencia es de consola y de base de datos,
y va transcrita en la columna Verif. de cada CA.

## Salvedades / follow-ups
- **F-SPEC-008-1 — CA-6 no se puede hacer desde aquí: necesita mano humana en Vercel.** En este entorno no hay CLI de Vercel (`vercel` no está instalado) ni token (`VERCEL_TOKEN` no está en `.env`) ni proyecto enlazado (no hay `.vercel/`), así que **ni las siete variables de Production ni la ausencia de variables en Preview ni el flujo 401/200/405/503 contra el despliegue se han tocado**. No se ha simulado nada. Lo que hace falta: (i) las siete variables en **Production** (`DATABASE_URL` del pooler transaccional, `API_FOOTBALL_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `INGEST_TICK_TOKEN`, `CRON_SECRET` con el mismo valor que el anterior); (ii) **ninguna** en Preview; (iii) desplegar la rama a producción para que existan `vercel.json` y la ruta con `GET`. Destino: el titular, antes del ensayo del miércoles 2026-09-23.
- **F-SPEC-008-2 — el job de pg_cron está vivo pero sin secretos, y por eso falla cada 30 s.** La migración de CA-4 está aplicada en `dev` y el job dispara puntualmente, pero `vault.decrypted_secrets` está vacío: `url` sale `null` y `net.http_post` revienta antes de encolar nada. Se arregla con **una** orden, `npm run cron:setup`, que exige antes dos líneas nuevas en `.env` del titular: `INGEST_TICK_URL=https://<dominio de producción>/api/ingest/tick` y `CRON_SECRET=<el mismo valor que INGEST_TICK_TOKEN>`. No he inventado la URL de producción a propósito: es dato del titular (la spec se lo asigna) y un valor mal puesto se traga el ensayo. Mientras tanto el job deja ruido en `cron.job_run_details`, que es exactamente lo que `npm run tick:salud` enseña.
- **F-SPEC-008-3 — `vitest.config.mts` gana el alias `server-only` → `tools/empty-module.mjs`.** Es el mismo alias que ya tenía `vitest.db.config.mts` (precedente F-SPEC-006-7) y hacía falta porque CA-1 y CA-2 obligan a importar `src/db/client.ts` y la ruta desde un test, y el paquete `server-only` lanza fuera de un React Server Component. No cambia nada del build: `next build` sigue viendo el `import "server-only"` real.
- **F-SPEC-008-4 — `src/db/schema.db.test.ts` pasa de `import { sql }` a `getSql()`.** Era el único consumidor del `sql` que CA-1 elimina; sin este cambio no compila. Está dentro de CA-1 pero el CA no lo nombra.
- **F-SPEC-008-5 — el workflow de CA-8 gana una entrada `cargar` en `workflow_dispatch`.** La verificación de CA-8 pide «el job `load` lanzado a mano», y con `load` condicionado solo a `push` un `workflow_dispatch` nunca lo ejecutaría. Con `cargar: si` corre solo `load`; con el valor por omisión (`no`) corre solo `sync`, como pide el CA.
- **F-SPEC-008-6 — CA-8 no es verificable hasta que el workflow esté en `main`.** GitHub solo ofrece `workflow_dispatch` (y solo respeta `schedule`) para ficheros presentes en la rama por defecto, así que los tres puntos de verificación de CA-8 —dispatch limpio sin PR, dispatch con cambio forzado que abre PR, y `load` a mano contra `dev`— **no se pueden ejecutar desde la rama**. El YAML sí está validado estructuralmente (parseado con Psych: `on.schedule[0].cron = '0 5 * * 2'`, `on.push.paths`, los dos jobs con sus `if`) y atado con tests de texto. Destino: verificación tras la fusión. **Actualizado tras O-1:** los tests ya no son de texto, sino aserciones sobre el YAML parseado.
- **F-SPEC-008-7 — O-2 era un fallo real mío, y estaba silencioso: arreglado (2026-09-21).** El paso «Sincronizar contra el proveedor» canalizaba `npm run calendario:sync -- "$TEMPORADA" | tee sync-diff.txt` sin `shell: bash`. El shell por omisión de GitHub en Linux es `bash -e {0}`, **sin `pipefail`**, así que el estado del paso era el de `tee` —siempre 0— y no el de `calendario:sync`, que sale 1 cuando el proveedor devuelve `errors`. Modo de fallo: el proveedor cae un martes, el paso «pasa», no hay diff, no se abre PR y la sincronización semanal no hace nada sin que nadie se entere. Arreglado con `shell: bash` (que da `bash --noprofile --norc -eo pipefail {0}`). **Es el único paso del fichero con tubería**: el de «¿Hay diff…?» no tiene ninguna y el de «Abrir o actualizar el PR» ya traía `set -euo pipefail` en su propio cuerpo. Hay test de regresión: «gives every piped step a shell with pipefail (O-2)» recorre los pasos de los dos jobs y exige `shell: bash` o `set -euo pipefail` a todo `run` con tubería. No es cambio de alcance: es un fallo de CA-8 encontrado en verificación.
- **F-SPEC-008-8 — O-1 resuelto sin dependencia nueva (2026-09-21).** Los tests de CA-8 pasan de `toContain` sobre el texto a aserciones sobre el árbol: `on.schedule[0].cron`, `on.push.branches`, `on.push.paths`, los `if` de los dos jobs y cada paso por su `name`. Como el proyecto no tiene parser de YAML y CA-10 prohíbe añadirlo (`node:yaml` no existe en Node 26 y `yaml` solo figura como peer opcional **no instalado** de Vite), `src/arch/deploy.test.ts` lee el subconjunto que usan los workflows —mapas y secuencias por indentación, secuencias de flujo, escalares entrecomillados y bloques `|`— en unas setenta líneas. Tres casos («SPEC-008 CA-8 the reader of the workflow») prueban primero al propio lector con los tres constructos delicados, para que las demás aserciones valgan algo. Comprobado que muerden: sin `shell: bash` cae exactamente el caso de O-2; con `branches: [master]` caen dos. `package-lock.json` intacto.
- **F-SPEC-008-9 — O-4 y O-5 cerrados (2026-09-21).** La fila CA-2 ya no dice que `src/ingest/` solo gane `engine.test.ts`: dice lo que era cierto, que ningún fichero **preexistente** se modificó salvo ese, y nombra los cuatro nuevos que piden CA-3 y CA-7. `src/db/schema.db.test.ts` importa ya `./client.ts` con extensión.
- **F-SPEC-008-10 — CA-3 no se cumplía: `npm run cron:setup` moría contra la base real (2026-09-22, arreglado).** `setupCronSecrets` buscaba los secretos con `where name = any(${sql.array(names)})` y Postgres respondía `op ANY/ALL (array) requires array on right side`, así que **el Vault se quedaba vacío y el job de pg_cron seguía fallando**: justo lo que CA-3 promete que deja resuelto. La causa no es un typo: `sql.array()` resuelve el tipo del array por `options.shared.typeArrayMap`, que postgres.js solo rellena cuando una conexión **termina** de abrirse (`connection.js`, `fetchArrayTypes`, vía `needsTypes`), y el parámetro se construye al montar la plantilla. En la **primera sentencia de un pool nuevo** —que es exactamente lo que hace `cron:setup`— el mapa está vacío, el valor se liga como `text` en vez de `text[]` y Postgres lo rechaza. Arreglado pasando un array JS plano, que se serializa sin ese mapa: corrige la causa, sobrevive en frío, no clava ningún OID a mano y mantiene la forma `any($1)`, así que los ocho casos del doble siguen valiendo.
- **F-SPEC-008-11 — la lección, que importa más que el typo: CA-3 se dio por bueno contra dobles.** Los ocho casos de `cron.test.ts` son verdes y ninguno tocó Postgres; la verificación llegó a ejecutar `cron:setup`, pero desde un cwd sin `.env`, así que abortaba en la comprobación de variables antes de llegar al SQL. **El camino real no corrió hasta que se ejecutó en producción**, con el ensayo a dos días. De ahí `src/ingest/cron.db.test.ts`. Y un matiz medido, no supuesto: **un test en transacción con rollback habría salido verde con el código viejo**, porque `begin` abre la conexión —y calienta el mapa de tipos— antes de que corra el callback. Comprobado revirtiendo el arreglo: de los tres casos nuevos solo cae el frío, con el `PostgresError` exacto de producción. Por eso el caso frío es de **solo lectura**: con los nombres reales en juego, un caso que escribiera pisaría los secretos del titular en cuanto `cron:setup` haya corrido de verdad.
- **F-SPEC-008-12 — el mismo defecto está latente en `src/ingest/engine.ts`, y no lo he tocado.** Las tres apariciones de `sql.array` (`insertDecision`, `resolveAlerts` y las cuatro consultas de `decideMatches`) se salvan hoy solo porque `decideMatches` corre **siempre** dentro de una transacción ya abierta, y para entonces el mapa está caliente; medido: los tres patrones en frío dentro de `sql.begin` funcionan, y el mismo patrón fuera de transacción falla. Era una propiedad de la que depende la corrección del motor y que no estaba escrita en ninguna parte: queda documentada en un comentario sobre `decideMatches`. El cambio de los tres a arrays planos es entrada de EPIC-MANT, no de esta spec.

- **F-SPEC-008-13 — dos pegas de usabilidad de `tick:salud`, encontradas al ejecutar el paso 5 de la puesta en marcha (2026-09-22, arregladas).** No son cambio de alcance de CA-7: los seis bloques, el cierre `REVISAR: …` y la redacción de secretos siguen intactos, y los seis casos originales de `salud.test.ts` pasan sin tocarlos. (i) **La lista de `FALLO` no tenía tope** y llegó a imprimir 116 líneas idénticas, enterrando los intentos, las alertas y los partidos en ventana, que es justo lo que hay que mirar el viernes. Ahora imprime **cinco**, las más recientes, y cierra con `… y M más`: el informe pasó de ~130 líneas a 31 y cabe en una pantalla. (ii) **Un fallo transitorio dejaba el semáforo rojo una hora entera.** Ahora el veredicto lo decide una **ventana corta de 10 minutos** y el resumen de la hora se queda como contexto informativo, de modo que un tick sano ahora se ve verde ahora sin ocultar que hubo problemas antes. Diez minutos son ~20 ejecuciones de un job que dispara cada 30 s —suficiente para que un tick sano se vea— y **menos que los 15 minutos de RN-05**, así que el informe se pone rojo antes de que el motor empiece a abrir alertas de silencio. Medido en `dev`: con el último fallo a las 22:26:32Z, el semáforo se pone verde hacia las 22:36:40Z en vez de a las 23:27Z. `salud.ts` sigue siendo puro: la ventana corta se deriva de `now` con `shiftInstant`, sin tocar el reloj.
- **F-SPEC-008-14 — CERRADO (2026-09-22): el silencio ya es rojo.** Abierto porque Si el job dejara de dispararse del todo, `tick:salud` saldría **verde**: `ok` solo mira fallos, y cero ejecuciones no es un fallo. CA-7 lo fija así de forma explícita («ninguna fila en ningún bloque → informe completo sin lanzar», con `ok: true`), y el caso está en los seis originales. Pero «pg_cron muerto» es exactamente lo que el semáforo debería cazar el viernes, y hoy no lo caza. Arreglarlo era decidir que sin ejecuciones recientes el informe es rojo, lo que cambiaba la letra de CA-7: no era mío. **Resuelto en la spec, no en el ledger**: el arquitecto enmendó CA-7 (N-6, `893ee01`) y Alberto lo aprobó el 2026-09-22, antes del ensayo. `ok: false` pasa a tener tres motivos, y el tercero es **cero ejecuciones en la ventana corta habiendo al menos un job en `cron.job`**; cuando es ese el que dispara el rojo, el cierre **nombra la razón**, porque un semáforo rojo sin motivo manda a cavar. Un job presente pero `INACTIVO` también es rojo: un tick apagado a mano no es una base nueva. Lo que sigue en verde es el caso legítimo que el sexto test original protegía —`cron.job` vacío, base recién creada—, ahora explícito en la condición en vez de implícito en la ausencia de reglas. Implementado con los once casos de la letra: escribí primero los tres que faltaban y los vi en rojo (`ok` no miraba `input.jobs`) antes de tocar `salud.ts`. `src/ingest/salud.ts` sigue puro: `input.jobs` ya le llegaba.

- **F-SPEC-008-15 — dos notas menores sobre la letra de los once casos de CA-7.** (i) El caso (3) pide «una `failed` de hace 90 min con la ventana corta limpia → `ok: true` **con la coletilla** “N problema(s) en la última hora”», y las dos mitades no pueden darse a la vez: un fallo de hace 90 minutos queda **fuera** de la hora, así que no se cuenta y no hay coletilla. Están cubiertas como dos casos separados —`ignores a failed run older than an hour` (sin coletilla, y lo afirma) y `stays green when the hour had failures but the short window is clean` (fallos a -30/-29, con la coletilla)—, que es el comportamiento que la letra describe leída en conjunto. (ii) El fixture del caso (3) hubo que completarlo: desde N-6, un job activo sin ninguna ejecución en diez minutos es rojo por sí solo, así que «ventana corta limpia» ya no significa «vacía» sino «con ejecuciones y ninguna fallida». Es consecuencia directa de la enmienda, no un desvío.

- **F-SPEC-008-16 — CA-8 (ii): el workflow NO puede abrir el PR. Bloqueante.** `gh workflow run calendario-semanal.yml --ref main` (run `35664756167`, 2026-09-22): el proveedor traía un aplazamiento real, el paso «Abrir o actualizar el PR» commiteó y empujó `chore/calendario-2026-09-21` y entonces murió con `pull request create failed: GraphQL: GitHub Actions is not permitted to create or approve pull requests (createPullRequest)`, **salida 1**. No es el YAML: declara `permissions: pull-requests: write`, pero el ajuste de repositorio manda. Confirmado con `gh api repos/tremen-dev/marcadorgal/actions/permissions/workflow` → `{"default_workflow_permissions":"read","can_approve_pull_request_reviews":false}`. **Arreglo (del titular, en Settings → Actions → General)**: activar «Allow GitHub Actions to create and approve pull requests», y subir `default_workflow_permissions` a `write` o dejar que el `permissions:` del job lo cubra. **Efecto colateral que he dejado en el remoto y no he limpiado** (juzgo, no reparo): la rama `chore/calendario-2026-09-21` existe en `origin` con un commit real de 5 ficheros (619+/619−) y sin PR. El titular decide si la borra o le abre el PR a mano. Un relanzamiento la reescribiría con `--force-with-lease`.
- **F-SPEC-008-17 — CA-8 (iii): el job `load` no tiene `DATABASE_URL`, y eso rompe la fusión del calendario, no solo la verificación. Bloqueante.** `gh workflow run … -f cargar=si` (run `35664983849`): el paso «Cargar el calendario en la base» sale **1** con `DATABASE_URL is not set`, y el log del paso muestra `DATABASE_URL:` vacío. `gh api repos/tremen-dev/marcadorgal/actions/secrets` → el **único** secreto del repositorio es `API_FOOTBALL_KEY`. `select count(*) from calendar_loads` → **5 antes y 5 después**. Lo grave no es el dispatch: el job `load` es el que corre `on: push` a `main` con `paths: [data/calendario/**, data/alias/**]`, así que **el día que se fusione el PR del calendario, la base no se cargará** y el fallo se verá como un check rojo en `main`. **Arreglo (del titular)**: añadir el secreto `DATABASE_URL` al repositorio con la cadena del pooler transaccional de `dev`, y relanzar el dispatch con `cargar=si` para ver la salida 0 y la fila nueva en `calendar_loads`.
- **F-SPEC-008-18 — CA-8 (i) no se pudo observar, y no lo he fabricado.** El CA pide «dispatch limpio sin cambios en el proveedor → termina limpio, sin PR». En el dispatch real el proveedor **sí** traía cambios, así que ese caso no se dio. **No he forzado ningún cambio en `data/calendario/`** para provocar el contrario, ni he revertido nada para provocar el caso limpio: queda pendiente de una semana en que el proveedor no mueva nada, o de comprobarse sobre el paso «¿Hay diff…?», que es la rama `cambios=no` del propio script.

- **F-SPEC-008-19 — CA-8: el push fallaba en cuanto la rama ya existía, que es el caso normal (2026-09-22, arreglado).** `git push --force-with-lease origin "$rama"` moría con `! [rejected] … (stale info)`: `actions/checkout@v4` deja un clon superficial **sin referencia de seguimiento** de esa rama, y `--force-with-lease` sin nada contra qué comparar aborta por seguridad. No era un caso raro: el propio paso contempla que el PR ya exista (`gh pr view` → `gh pr edit`), así que la rama preexistente es lo esperado a partir del segundo martes. **Arreglado sin forzar más, sino forzando menos**: `fetch-depth: 0` en el checkout, y el paso **construye el commit encima de la rama remota** (`git fetch` + `git checkout -f -B "$rama" refs/remotes/origin/$rama`, restaurando encima el resultado del sync) en vez de reescribirla. El push pasa a ser un **avance rápido** y `--force` desaparece del fichero. Propiedades que se ganan: (i) nada de lo que viva en esa rama se pierde —si alguien corrigió algo encima, su commit sigue en la historia—; (ii) si alguien empuja entre el fetch y el push, el push se **rechaza** y el job falla, que es exactamente lo que tiene que pasar; (iii) funciona igual exista la rama o no, por las dos ramas del `git ls-remote`. La rama `chore/calendario-2026-09-21` que quedó en `origin` no se reescribe: se le añade un commit encima. Y si la rama ya trae ese mismo calendario, no se commitea nada y el PR solo se actualiza.
- **F-SPEC-008-20 — O-6: el aplazamiento ya no se entierra. De 619+/619− en cinco ficheros a UNA línea (2026-09-22).** La causa no era que faltara orden, sino que el orden era **por `kickoff`**: un aplazamiento mueve el partido de sitio y recoloca el bloque entero, y es justo el evento que CA-8 existe para poner delante de un humano (D-3). Ahora los partidos se ordenan por `round + home + away` —el id derivado del partido, que no cambia nunca—, así que un aplazamiento **reescribe un `kickoff` en su sitio**. De paso, las comparaciones de texto pasan de `localeCompare` (dependiente del locale de la máquina, y el sync corre en un runner mientras la revisión se hace en un portátil) a orden de code point. Reproducido antes y después contra el proveedor real: **antes** 5 ficheros y 619+/619−; **después** `1 file changed, 1 insertion(+), 1 deletion(-)`, y esa línea es el aplazamiento (`levante`–`athletic-club`, `2026-09-16T19:30:00Z` → `2026-10-21T18:00:00Z`).
- **F-SPEC-008-21 — la normalización de `data/`, en un commit aparte y revertible (`332bce8`).** Los ficheros venían de `tools/aplicar-revision-nombres.mjs` y nunca se habían vuelto a escribir con el criterio del escritor, así que la primera pasada del sync los reordenaba enteros. Normalizarlos cuesta **1462+/1462−** en los cinco, y **no cambia ni un dato**: comprobado que `competition`, `teams` y `matches` son idénticos como conjunto antes y después en los cinco ficheros, y `npm run calendario:load -- 2026-27` contra `dev` da **0 insertados, 0 actualizados, 1760 sin cambios** en las cinco competiciones. Va aparte a propósito: sin ella el ruido lo pagaría igual el primer PR del sync, que es justo el que tiene que leer un humano; con ella, ese PR es el aplazamiento y nada más. Si no se quiere, `git revert 332bce8` y el resto del trabajo no se toca.
- **F-SPEC-008-18 — CERRADO (2026-09-22): el caso limpio observado, sin fabricar nada.** Con el calendario ya al día —aplicado el aplazamiento en un commit temporal que después descarté— una segunda pasada de `npm run calendario:sync -- 2026-27` sale **0** y deja `git status --porcelain data/calendario data/alias` **vacío**, que es exactamente la rama `cambios=no` del paso «¿Hay diff…?»: termina limpio y no abre PR. No forcé ningún cambio en `data/calendario/` ni revertí nada para provocarlo; lo que hice fue ponerme en el estado en que el proveedor no trae novedades, que es la definición del caso. Queda por ver en el propio workflow, que es del verificador.


### Observaciones del verificador (2026-09-21)
Ninguna bloquea la spec; las dos primeras van al titular **antes** de la
primera ejecución real del workflow.

- **O-1 — los tests de CA-8 son `toContain` sobre el texto del YAML, no sobre
  su estructura.** `src/arch/deploy.test.ts` afirma cadenas sueltas
  (`"branches: [main]"`, `"workflow_dispatch:"`), que no prueban **dónde**
  están: `branches` podría acabar colgando de `schedule` y el test seguiría en
  verde, y un `workflow_dispatch:` dentro de un comentario también pasaría. He
  parseado el fichero yo con Psych y la estructura **es** correcta hoy, así que
  el artefacto está bien y el CA no exige test alguno; lo que digo es que estos
  tests no cazarían una regresión estructural. Al verificar CA-8 tras la
  fusión, conviene cambiarlos por aserciones sobre el YAML parseado.
- **O-2 — el paso de sincronización se traga un fallo del proveedor.** En
  `calendario-semanal.yml`, el paso «Sincronizar contra el proveedor» es
  `npm run calendario:sync -- "$TEMPORADA" | tee sync-diff.txt` sin declarar
  `shell: bash`. El shell por omisión de GitHub Actions es `bash -e {0}`, **sin
  `pipefail`**, así que el estado de salida del paso es el de `tee` (siempre 0)
  y no el de `calendario:sync`, que sale 1 cuando el proveedor devuelve
  `errors` o la validación falla. Modo de fallo: el proveedor cae un martes →
  el paso «pasa» → no hay diff → no se abre PR → la sincronización semanal no
  hace nada y **nadie se entera**. Arreglo de una línea: `shell: bash` en ese
  paso (o `set -o pipefail`).
- **O-3 — `tickSalud` cuenta como fallo cualquier `status !== 'succeeded'`.**
  Una ejecución en curso (`running`) se lee como `FALLO` y tira el `ok` a
  `false`. Es conservador en la dirección buena (avisa de más, no de menos),
  pero el viernes puede dar un `REVISAR` falso si `npm run tick:salud` cae
  justo mientras un job está en vuelo: conviene saberlo antes de asustarse.
- **O-4 — inexactitud en el ledger del implementador, fila CA-2.** Dice
  «`git diff main -- src/ingest` solo `engine.test.ts`». No es así:
  `src/ingest/` gana cuatro ficheros nuevos (`cron.ts`, `cron.test.ts`,
  `salud.ts`, `salud.test.ts`), que los propios CA-3 y CA-7 exigen. Lo que sí
  es cierto —y es lo que CA-2 quiere decir— es que **ningún fichero
  preexistente** de `src/ingest/` se modifica salvo `engine.test.ts`, que es
  CA-9. La afirmación sustantiva se sostiene; la frase, tal como está escrita,
  no.
- **O-5 — nit heredado.** `src/db/schema.db.test.ts` sigue importando
  `from "./client"` sin `.ts`. Viene de `main` y CA-10 solo exige la extensión
  en los ficheros **nuevos**, así que no es incumplimiento; pero F-SPEC-008-4
  tocó justo esa línea y era gratis dejarla arreglada.
- **F-SPEC-008-22 — CA-8 no se ha ejecutado nunca de punta a punta, y por eso la 3ª vuelta es RED.** Las dos causas que lo tumbaron (la política de PRs y el secreto `DATABASE_URL`) están fuera, comprobado por API; el push nuevo lo he simulado en cinco escenarios y se comporta; el `sync` y el `load` los he corrido de verdad y salen 0. Pero **el workflow con este YAML no ha corrido nunca**: la rama `ft/SPEC-008-cron-setup-array` no está en `origin`, así que no hay ref contra el que dispararlo, y mi propio `gh workflow run` fue denegado por el clasificador de permisos del entorno. **Este workflow no ha abierto un PR ni una sola vez.** Dos veces ya el artefacto parecía correcto y falló en ejecución por motivos distintos (F-SPEC-008-16, luego F-SPEC-008-19), así que sobre simulación no lo doy por cumplido. **Lo que lo cierra, y es barato**: empujar la rama y `gh workflow run calendario-semanal.yml --ref ft/SPEC-008-cron-setup-array` —basta con ver el PR abierto con el diff de una línea— o fusionar y lanzarlo contra `main`. Queda una interacción sin probar: `default_workflow_permissions` sigue en `read` y el permiso de escritura lo pone el bloque `permissions:` del job; debería bastar, pero es justo lo que no se ha visto correr.
- **F-SPEC-008-21, corrección de dato: son 1834 partidos, no 1760.** Mi comprobación **independiente** de la normalización, por camino distinto al del implementador: comparé `main` contra la rama con una serialización canónica (claves de cada objeto ordenadas recursivamente, para que un cambio de orden de claves no pueda tapar un cambio de valor) y contrasté los tres bloques como **multiconjunto**. Resultado en los cinco ficheros: `competition` idéntico, claves de primer nivel idénticas, `teams` **98** elementos y `matches` **1834** elementos con **multiconjunto idéntico**, cero elementos solo en un lado, cero conteos distintos y cero duplicados en ninguno de los dos lados. Y por un tercer camino, `npm run calendario:load -- 2026-27` contra `dev`: **0 insertados, 0 actualizados, 1834 sin cambios, 0 huérfanos**. **La normalización no cambia ni un dato.** El número del ledger («1760 sin cambios») no cuadra con lo que devuelve la herramienta hoy: son **1834**. Corrígelo.


### Observaciones del verificador (2026-09-22, 2ª vuelta)
Ninguna de las tres bloquea por sí sola; la primera conviene mirarla **antes**
de que el PR semanal del calendario empiece a llegar.

- **O-6 — el `sync` reordena `teams` y entierra el aplazamiento en 619 líneas
  de ruido.** El diff que produjo el dispatch real son 5 ficheros y
  **619+/619−**, y el número de partidos no cambia en ninguno (380, 380, 462,
  306, 306 antes y después). Lo sustantivo es **un** aplazamiento
  (`levante`–`athletic-club`, de `2026-09-16T19:30Z` a `2026-10-21T18:00Z`) y
  dos altas de equipo en `primera-rfef-g1` (`aviles`, `logrones`); el resto es
  el array `teams` reordenado —`bilbao-athletic` y `aviles` se mueven de sitio—
  y partidos recolocados por orden de `kickoff`. CA-8 existe para que «un humano
  vea los aplazamientos antes de que entren en base» (D-3), y con este formato
  de diff el humano no los ve. Ordenar `teams` y `matches` de forma estable al
  escribir dejaría el PR en las tres o cuatro líneas que importan. No es
  incumplimiento de la letra de CA-8: es lo que hace que CA-8 sirva de algo.
- **O-7 — el caso (3) de la letra de CA-7 es contradictorio, y el ledger ya lo
  documenta.** «Una `failed` de hace 90 min con la ventana corta limpia →
  `ok: true` **con la coletilla** “N problema(s) en la última hora”» no puede
  darse: 90 minutos cae fuera de la hora y no se cuenta. Verificado que las dos
  mitades están cubiertas por dos casos reales (`ignores a failed run older than
  an hour`, que además afirma la **ausencia** de coletilla, y `stays green when
  the hour had failures but the short window is clean`, con fallos a −30/−29 y
  la coletilla `2 problema(s)`). Doy CA-7 por cumplido: la propiedad está
  probada en las dos direcciones y el defecto es de redacción del CA, no del
  código. Queda dicho por si el arquitecto quiere afinar la letra.
- **O-3 sigue vigente y hoy se puede medir**: `tickSalud` cuenta cualquier
  `status !== 'succeeded'` como fallo, `running` incluido. Con el job vivo y
  disparando cada 30 s, la probabilidad de que `npm run tick:salud` pille una
  ejecución en vuelo el viernes ya no es teórica. Avisa de más, no de menos,
  pero conviene saberlo antes de asustarse.

### Observaciones del verificador (2026-09-22, 3ª vuelta)

- **O-8 — «nada de lo que viva en esa rama se pierde» (F-SPEC-008-19) es cierto para la *historia*, no para el *contenido* de `data/`.** Medido en el escenario (E): si alguien corrige a mano un fichero de `data/calendario/` **dentro** de la rama `chore/calendario-<fecha>`, la siguiente pasada del sync lo **revierte**, porque el paso borra `data/calendario` y `data/alias` y los repuebla con el resultado del sync, que se calculó sobre el árbol del ref del dispatch y no sobre la rama. El commit del humano sigue en la historia y la reversión queda **visible como diff** en el commit nuevo, así que no es silencioso, y es estrictamente mejor que el `--force-with-lease` anterior, que se llevaba el commit entero por delante. Además es la semántica razonable de un fichero generado: la corrección va aguas arriba (alias o proveedor), no en la rama. **No es un defecto**; lo anoto para que la frase del ledger no se lea de más.
- **O-9 — quedan dos `localeCompare`, y ninguno decide el contenido del calendario.** `src/calendar/sync.ts:196` ordena `diff.newTeams`, que solo va al **informe** del PR (`formatSyncDiff`), no a los JSON: el orden de esas líneas del cuerpo sigue dependiendo del locale de la máquina que corra el sync. `src/calendar/sync.ts:212` ordena los alias de partido **con locale explícito** (`localeCompare(b, "en", {numeric: true})`), que es determinista y por tanto no contradice la nota de F-SPEC-008-20. Queda también `src/sources/api-football/calendar.ts:98`, que ordena lo **importado** y cuyo efecto anulan los dos `sort` del escritor; y es `src/sources/`, que CA-10 prohíbe tocar. Conclusión: la afirmación «el locale de la máquina no decide cómo se ve el repo» **se sostiene para los ficheros de calendario**. El del informe es cosmético y no urge.

### Residual de SPEC-008 al cerrar (2026-09-22)

- **R-SPEC-008-2 — la mitad de CA-5 y CA-6 que vive en el panel de Vercel.**
  Destino: **ojo humano del titular**, no una spec. Concretamente: que el cron de
  `vercel.json` figure **activo** en el panel con su última ejecución en 200 y
  una invocación por minuto con `inWindow: 0` en los logs (CA-5); el inventario
  de las **siete** variables en **Production** y **ninguna** en **Preview**
  (CA-6 i); y el **503** de un despliegue de preview (CA-6 ii), que hoy devuelve
  **302** porque la Deployment Protection está activa y que ya se midió
  puntualmente desactivándola. Nada de esto bloquea el ensayo ni la jornada: el
  camino que de verdad importa —el endpoint de producción respondiendo 200 al
  `GET` con Bearer— está probado, y pg_cron lo ejerce cada 30 s.
- **O-9 (menor) — el orden de `diff.newTeams` en el cuerpo del PR sigue
  dependiendo del locale** (`src/calendar/sync.ts:196`). Solo afecta al informe,
  nunca a los ficheros. Destino: **EPIC-MANT**, junto con las tres
  `sql.array` de `src/ingest/engine.ts` (F-SPEC-008-12).
- **El PR #10 y la rama `chore/calendario-2026-09-21` son artefacto de la
  prueba**, contra una base sin normalizar. CA-8 dice literalmente «el PR se crea
  con el diff esperado y **se cierra sin fusionar**», así que cerrarlo es lo que
  el CA prescribe, no un apaño. Recomendación del verificador: **borrar también
  la rama** al cerrar el PR, para que ningún dispatch posterior del mismo día la
  reutilice como base obsoleta. Lo gestiona el orquestador.

## Cómo retomar (handoff)
**Hecho y verde en local**: CA-1, CA-2, CA-3, CA-4, CA-5, CA-7, CA-9 y CA-10, con `npm run gates` y `npm run test:db` los dos en salida 0. Nueve commits en `ft/SPEC-008-despliegue-del-tick`, uno por CA, sin push ni PR.

**El riesgo declarado de la épica está despejado**: pg_cron **1.6.4** admite `'30 seconds'` y el job dispara cada 30 s exactos contra `dev`. No hace falta el fallback de H-3 ni plantearse los 60 s.

**Lo que queda, y es todo humano**, en este orden y antes del miércoles 2026-09-23:
1. Añadir a `.env` `INGEST_TICK_URL` (URL absoluta de `/api/ingest/tick` en producción) y `CRON_SECRET` (mismo valor que `INGEST_TICK_TOKEN`).
2. Poner las siete variables de CA-6 en **Production** de Vercel y **ninguna** en Preview.
3. Fusionar la rama y desplegar a producción, para que existan `vercel.json`, el cron de respaldo y el `GET` de la ruta.
4. `npm run cron:setup` una vez contra `dev`: deja los dos secretos en Vault y el job deja de fallar.
5. `npm run tick:salud`: debe salir **0** con `succeeded: 100%` y `partidos en ventana: 0`. Ese es el semáforo del ensayo.
6. Verificar CA-6 (401/200/405 contra producción, 503 contra un preview, `ingest_attempts` sin crecer) y CA-8 (`workflow_dispatch` del calendario, ya en `main`).

**Nada de esto es código**: si el paso 5 sale en verde, el tick está desplegado y SPEC-009 hereda una casa lista para medir el viernes.
