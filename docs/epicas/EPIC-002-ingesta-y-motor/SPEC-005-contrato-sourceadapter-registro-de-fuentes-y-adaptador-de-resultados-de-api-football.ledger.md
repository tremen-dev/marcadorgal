---
id: SPEC-005
tipo: ledger
epica: EPIC-002
---
# Ledger — SPEC-005 Contrato SourceAdapter, registro de fuentes y adaptador de resultados de API-Football

## Resumen
- Fase: en-revision (implementación completa el 2026-09-21; pendiente la verificación y el fixture `live-<fecha>.json` de CA-7 (iii), F-SPEC-005-1)
- Rama: `ft/SPEC-005-contrato-sourceadapter-registro-de-fuentes-y-adaptador-de-resultados-de-api-football`

## Matriz de criterios de aceptación
<!-- Escritores: sdd-implementador rellena Implementado y Test; sdd-verificador rellena Verif. y Estado. Nunca al revés. -->
<!-- Estados por CA: ✅ cerrado · ⚠️ parcial/con salvedad · 🚧 en curso · ❌ sin empezar · n-a -->
<!-- Un CA está ✅ solo cuando Implementado + Test + Verif. aplicables están en verde. Una salvedad se marca ⚠️, nunca ✅. -->
| CA | Implementado (fichero) | Test (fichero/caso) | Verif. | Estado |
|---|---|---|---|---|
| CA-1 | `src/model/source.ts` (`WindowMatch`, `FetchContext`, `RawRequest`, `RawCapture`, `ParsedObservation`, `ExternalTeam`, `Unresolved`, `UnresolvedReason`, `Skipped`, `SkippedReason`, `ParseResult`, `interface SourceAdapter`), reexportado por `src/model/index.ts`; `src/model/state.ts` (`AddedMinute` 1..30, rama `live` con `addedMinute: AddedMinute.nullable()`, `LiveMinute`, `compareLiveMinute` lexicográfico de `(minute ?? -1, addedMinute ?? 0)`). Las claves de núcleo (`id`, `sourceId`, `receivedAt`, `rawRef`) en `ParsedObservation` y `addedMinute` fuera de `live` se rechazan con `z.never().optional()` porque `z.object` descarta claves desconocidas (F-SPEC-005-2) | `src/model/source.test.ts` (12 casos: `live` sin `addedMinute`, con 0/31/1.5, `finished`/`scheduled` con `addedMinute` → rechazados; `45+3` y `null/null` → aceptados; `compareLiveMinute` negativo en los tres pares y 0 en `(90,null)` vs `(90,null)`; `@ts-expect-error` en literal `SourceAdapter` sin `parse` (sin la línea `npm run typecheck` da TS2741); `ParseResult` válido, rechaza `scheduled` con `score`, `rawRef`/`sourceId` en `ParsedObservation`, `reason` fuera de lista; `RawCapture` con `requests: []`); `src/model/model.test.ts` (fixtures `live` con `addedMinute: null`, caso N-8 de rechazos) | | ❌ |
| CA-2 | `src/model/source.ts` (`SourceConfig` strictObject + `superRefine` claves de `priority` = `competitions`; `FEDERATION_PRIORITY = 50`, `OPERATOR_PRIORITY = 100`); `src/sources/registry.ts` (`SOURCES = SourceConfig.array().parse([...])`, `sourceConfig(id)`; una entrada `api-football`: pull, cinco competiciones a 10, 30 s, `marcador.gal (ingesta; https://marcador.gal)`, `legalBasis` = borrador H-1, F-SPEC-005-4); solo importa `src/model` | `src/sources/registry.test.ts` (10 casos: entrada única con esos valores; `sourceConfig`; bandas 50/100; `SourceConfig` acepta válido y rechaza `minIntervalSeconds: 0`, prioridad de competición no cubierta, competición cubierta sin prioridad, prioridad 100 y 0, `legalBasis: ''`, `competitions: []`, clave extra) | | |
| CA-3 | `src/model/alias.ts` (`AliasEntry`, `AliasFile` con `matches?: Record<string, MatchId>`), reexportado por `src/model/index.ts` y `src/calendar/schema.ts`; `validateAliases` con `knownMatches` (valor desconocido o repetido → issue `matches.<externalId>`); `src/calendar/files.ts` pasa los ids derivados de los calendarios; `src/model/calendar-import.ts` (`ImportedMatch.externalId`); `src/sources/api-football/calendar.ts` (`externalId = String(fixture.id)`); `src/calendar/sync.ts` (regla g: `matches[externalId] = id derivado`, conserva ausentes, sobrescribe y `diff.rematched`, orden numérico por clave, F-SPEC-005-7; `formatSyncDiff` imprime `rematched`); `tools/calendario-sync.mjs` pasa `knownMatches` | `src/calendar/schema.test.ts` «SPEC-005 CA-3» (6: identidad con `src/model`, sin `matches` → `[]`, válidos, valor fuera de `knownMatches`, `MatchId` repetido, clave vacía y valor no slug); `src/calendar/sync.test.ts` (regla g, entradas ausentes conservadas y orden, `rematched`; idempotencia con `rematched: []`); `src/sources/api-football/calendar.test.ts` («fills externalId with fixture.id as a string»); `src/calendar/files.test.ts` (alias con `matches` válido → sin issues, inválido → `matches.7`); `src/calendar/cli.test.ts` (`formatSyncDiff` con `rematched: 1`) | | |
| CA-4 | `data/alias/2026-27/api-football.json`: `matches` con 1834 entradas (`npm run calendario:sync -- 2026-27`, 2026-09-21, plan Pro, 5 peticiones). Diff del fichero: solo `+ "matches": {...}`; ninguna entrada de `teams` cambia. El sync no propuso `newTeams`, `rescheduled` ni `missing` (solo `unconfirmed 1`, el TBD de F-SPEC-004-11, ya `PST` en el proveedor). Los cuatro calendarios que el sync reescribiría en su orden canónico (contenido idéntico) se revirtieron: F-SPEC-005-3 | `src/calendar/data.test.ts` («maps every match of the season exactly once and nothing else (SPEC-005 CA-4)»: valores de `matches` sin repetir, cada id derivado es valor y viceversa, 1834 = 1834); `git grep -qF "$API_FOOTBALL_KEY"` → sin coincidencias | | |
| CA-5 | `src/sources/api-football/results.ts` (`createApiFootballResults({ aliases, apiKey })`: `id = 'api-football'`, `kind = 'pull'`; `fetch(ctx)`: ventana vacía → `requests: []` sin llamadas; `live=<ligas ascendentes>` solo si algún `kickoff <= now` (N-10); `ids=<hasta 20 ascendentes>` para los `fixture.id` (inversa de `aliases.matches`) ausentes del cuerpo de `live=`; partidos sin alias omitidos; cabeceras `x-apisports-key` y `User-Agent`; `RawRequest` con `url`, `status`, `content-type` y `res.text()`; `!res.ok` lanza `api-football responded <status>`; sin `Date.now()`/`new Date()`) | `src/sources/api-football/results.test.ts` «CA-5» (8 casos con stub que registra llamadas, alias y calendario reales: ventana vacía → 0 llamadas; 3 partidos de dos competiciones con uno en `live=` → `?live=141-439` y `?ids=<los otros dos>`; 25 partidos → `live=` + `ids=` de 20 y 5; todos con `kickoff > now` → solo `ids=`; partido sin alias omitido; cabeceras en todas y clave ausente de `JSON.stringify(raw)`; 429 → rechaza) | | |
| CA-6 | `src/sources/api-football/results.ts` (`parse`: `JSON.parse` por cuerpo, `looseObject`, `errors` no vacío lanza, `fixture.id` repetido cuenta una vez (la última); identidad todo-o-nada `unknown_competition` → `unknown_team` → `unknown_match` → `inconsistent_alias`; tabla `STATUS` N-7; `score` solo en `live/finished/suspended` (gol nulo → `skipped missing_score`); `minute`/`addedMinute` solo en `live` (0 → `null`, > 30 acotado a 30); estado desconocido → `skipped unsupported_status`; sin `observedAt`; `resolveTeam` por `externalId`, ignora la competición) | `results.test.ts` «CA-6 resolveTeam» (538 → `celta` en dos competiciones, inexistente → `null`), «CA-6 parse is pure» (fetch que lanza, `ParseResult` válido sobre el fixture real sin `observedAt`, `errors` objeto y array → lanza, cuerpo no JSON / no fixtures → lanza, duplicado cuenta una vez), «CA-6 state mapping» (las 19 filas de N-7 por `it.each` sobre el partido real 1569926; `NS` con goles → `score: null`; `HT` `extra: null` y `2H` `extra: 0` → `null`; `LIVE` sin `elapsed`; `extra: 45` → 30; `FT` real con `extra: 5` → `minute: null` sin `addedMinute`; `XX` → skipped; gol nulo → `missing_score`), «CA-6 identity is all-or-nothing» (`unknown_team` con nombres externos, `unknown_match`, `inconsistent_alias` por par de equipos y por competición, `unknown_competition` con liga 39, cuerpo mixto conserva los buenos) | | |
| CA-7 | `src/sources/api-football/fixtures/ids-2026-09-21.json` (i: 15 partidos, 10 `FT` de las cinco ligas con `extra` real, 4 `NS`, 1 `PST` real de Levante - Athletic, 2026-09-21T12:42:48Z), `live-all-2026-09-21.json` (ii: 9 partidos ajenos, `2H` con `extra` 1 y 2, `HT` con `extra` real 3/3/6, `1H`; 2026-09-21T12:51:12Z), `fixtures/README.md` (comando, fecha, recorte). (iii) `live-<fecha>.json` **pendiente** hasta el 2026-09-25 18:30Z (F-SPEC-005-1); el caso «live resuelto» se deriva en memoria | `results.test.ts` «CA-7 real fixtures parse» (3: (i) `finished` con marcador y `scheduled` sin él para los ids esperados, `postponed` de Levante - Athletic, ≥ 3 `FT`, ≥ 3 `NS`, `PST`, cinco ligas; (ii) todo `unresolved unknown_competition` sin excepción, `ParseResult` válido, algún `1H/2H` con `extra`; (ii→live resuelto) un `1H` con `extra` reidentificado como Albacete - Córdoba → `live` con `minute` y `addedMinute` del proveedor) | | |
| CA-8 | `src/arch/source-contract.test.ts` (adaptador `memory` pull con cuerpo propio, `orphan` sin configuración, driver `drive(configs, adapters, ctx)` que solo usa `SourceAdapter`/`SourceConfig`; importa `@/model`, `../sources/registry.ts` y `../sources/api-football/results.ts`) | El mismo fichero (1 caso: `ParseResult` válido para `api-football` y `memory`, `ParsedObservation` de `segunda-division-2026-27-j6-albacete-cordoba` en ambos, `orphan.fetch` no llamado, `live=141` e `ids=1569926` pedidos, una llamada a la URL de `memory`) | | |
| CA-9 | `src/arch/sources-boundary.ts` (fichero directamente bajo `src/sources/`: imports relativos del mismo nivel, nunca `./<id>/…`); imports relativos con `.ts` en todos los ficheros nuevos y tocados; `package.json` sin cambios (`git diff main -- package.json` vacío) | `src/arch/sources-boundary.test.ts` «SPEC-005 CA-9» (2: `./registry.ts` desde `src/sources/registry.test.ts` aceptado; `./api-football/results.ts` desde `src/sources/registry.ts` rechazado) y «src/sources/**/*.{ts,tsx} has no violations» con el árbol real; `node --input-type=module -e "await import('./src/sources/registry.ts'); await import('./src/sources/api-football/results.ts')"` → exit 0 (aviso `MODULE_TYPELESS_PACKAGE_JSON`, F-SPEC-005-8); `grep -rE "from ['\"](postgres\|next\|@/db)" src/sources` → vacío; `grep -nE "Date\.now\(\)\|new Date\(" results.ts` → vacío; `git grep -qF "$API_FOOTBALL_KEY"` → sin coincidencias; `env -u DATABASE_URL -u API_FOOTBALL_KEY npm ci && npm run gates` → exit 0 el 2026-09-21 (Biome 82 ficheros, Vitest 23 ficheros / 299 casos, `next build` ok; Node 26.4.0, F-SPEC-005-8) | | |

## Veredicto del verificador
<!-- GREEN/RED + fecha + resumen. Lo escribe SOLO sdd-verificador. -->

## Evidencia visual
<!-- Tabla CA → captura en _qa/SPEC-005/. Informe HTML opcional: _qa/SPEC-005/informe.html -->
n-a: la spec no tiene interfaz; la evidencia es la salida de comandos y los fixtures.

## Salvedades / follow-ups
<!-- IDs F-SPEC-005-1, F-SPEC-005-2… con destino (spec futura o EPIC-MEJORA). -->
- **F-SPEC-005-1** CA-7 (iii): `live-<fecha>.json` (`GET /fixtures?live=140-141-435-875-439` con partidos de las cinco ligas en juego) no se ha capturado: la primera ventana es el viernes 2026-09-25 18:30Z o después y la implementación termina el 2026-09-21. El caso «live resuelto» se deriva en memoria (test «(ii) a live=all fixture with extra, given our identity, yields live with addedMinute»). **Fecha de captura pendiente**; al capturarlo, añadir la fila al README y un test que lo parsee con `parse` (estados presentes tal cual). Destino: esta spec antes de cerrar, o SPEC-006 si el humano prefiere no esperar.
- **F-SPEC-005-2** CA-1: la definición literal `ParsedObservation = MatchState.and(z.object({ matchId, observedAt? }))` no puede rechazar `rawRef`/`sourceId` (ni las ramas no-`live` `addedMinute`) porque `z.object` descarta claves desconocidas y `strictObject` en una intersección rechazaría las claves de la otra mitad. Se declaran `id`, `sourceId`, `receivedAt`, `rawRef` (en `ParsedObservation`) y `addedMinute` (en `scheduled/finished/postponed/suspended`) como `z.never().optional()`: presentes → rechazo, ausentes → nada. Tipo inferido: `key?: never`. Informativo.
- **F-SPEC-005-3** CA-4: `syncCalendar` escribe `teams` ordenados por `id` y `matches` por `(round, kickoff, home)`; los ficheros revisados en H-2 de SPEC-004 no siguen ese orden en cuatro competiciones (`primera-rfef-g1`, `segunda-division`, `segunda-rfef-g1`, `tercera-rfef-g1`), así que el sync los reescribe con contenido idéntico (mismo conjunto de equipos y partidos, comprobado). Se revirtieron para que el diff de CA-4 sea solo el alias; el próximo `calendario:sync` sin `--dry-run` los reordenará (sin efecto en `calendario:load`, que es idempotente por id). Informativo.
- **F-SPEC-005-4** CA-2 / H-1: `legalBasis` lleva el borrador de la spec («Plan Pro de API-Football contratado el 2026-09-21 por el titular; uso conforme a los términos del proveedor; gestión fuera del repo (D-7).»). El titular valida o redacta el texto definitivo (D-7).
- **F-SPEC-005-5** CA-7 (i): `ids-2026-09-21.json` va sin recorte (198 KB): `GET /fixtures?ids=` devuelve además `events`, `lineups`, `statistics` y `players` por partido, que el adaptador ignora (fuera de alcance). El partido `TBD` del calendario (`primera-division-2026-27-j6-levante-athletic-club`) figura ya como `PST` en el proveedor, así que el fixture tiene un `PST` real y el caso `TBD` se prueba cambiando `status.short` en memoria (precedente F-SPEC-004-1). El sync sigue marcándolo `unconfirmed` (F-SPEC-004-11). Informativo.
- **F-SPEC-005-6** CA-7 (ii): en `live-all-2026-09-21.json` (12:51:12Z, tercera captura de la tarde; las dos anteriores, 12:42Z y 12:48Z, tenían menos variedad) no había ningún partido en `ET` ni `P` en todo el proveedor; sí `2H` con `extra` (90+1, 90+2), `HT` con `extra` real (el proveedor conserva el añadido del primer tiempo durante el descanso: `45+3`, `45+6`) y `1H`. `ET` y `P` se prueban en memoria (`it.each` de CA-6). Informativo.
- **F-SPEC-005-7** CA-3: «`matches` sale ordenado por clave» se implementa con orden numérico (`localeCompare` con `numeric: true`), porque los objetos JS listan las claves con forma de entero en orden numérico ascendente sean cuales sean la inserción y el orden pedido; es el orden que `JSON.stringify` escribe. `formatSyncDiff` imprime `rematched` (contador y líneas), fuera del texto literal de la spec pero necesario para el comando. Informativo.
- **F-SPEC-005-8** Gates y comandos ejecutados en local con Node 26.4.0 (`.nvmrc` 24), como F-SPEC-004-10; el import directo con `node --input-type=module` emite el aviso `MODULE_TYPELESS_PACKAGE_JSON` (F-SPEC-004-3, destino EPIC-MEJORA). Informativo.
- **F-SPEC-005-9** CA-6: `parse` acepta `fixture.id`, `league.id` y `teams.*.id` como entero o cadena (el proveedor da enteros) y los normaliza con `String()`; `status.elapsed`/`extra` pueden faltar (`optional`) además de ser `null`. Informativo.

## Cómo retomar (handoff)
<!-- Estado real del trabajo para la siguiente sesión: qué está hecho, qué falta, dónde seguir. -->
- **Hecho (2026-09-21, rama `ft/SPEC-005-contrato-sourceadapter-registro-de-fuentes-y-adaptador-de-resultados-de-api-football`):** CA-1..CA-9 implementados con test; commits por CA (`308d957` CA-1, `5c5ee98` CA-2 + frontera CA-9, `58759dc` CA-3, `630e59e` CA-4, `be87276` CA-5, `6dae8bb` CA-6, `f808066` CA-8, `8a8f8f7` CA-7, y el último con ledger y transición a en-revision). `npm test` 23 ficheros / 299 casos; `npm run typecheck` y `npm run lint` limpios; `env -u DATABASE_URL -u API_FOOTBALL_KEY npm ci && npm run gates` → exit 0 (Node 26.4.0). Sin push, sin PR.
- **Pendiente humano:** validar `legalBasis` (F-SPEC-005-4); decidir si el fixture `live-<fecha>.json` (F-SPEC-005-1) se captura antes de cerrar (viernes 2026-09-25 desde 18:30Z: `curl … "fixtures?live=140-141-435-875-439"`, guardar en `src/sources/api-football/fixtures/live-2026-09-25.json`, fila en el README, test en `results.test.ts` «CA-7») o pasa a SPEC-006.
- **Para el verificador:** reproducir `npm run gates`; el bucle `git grep -qF` de la clave; `node --input-type=module -e "await import('./src/sources/registry.ts'); await import('./src/sources/api-football/results.ts')"`; `npm run calendario:sync -- 2026-27 --dry-run` (5 peticiones; propondrá la reordenación de F-SPEC-005-3 y `unconfirmed 1`, sin cambios de `name`/`shortName`/`teams`/`matches`); comprobar que `data/alias/2026-27/api-football.json` tiene 1834 `matches`. La base de datos no se toca en esta spec (`added_minute` es de SPEC-006).
