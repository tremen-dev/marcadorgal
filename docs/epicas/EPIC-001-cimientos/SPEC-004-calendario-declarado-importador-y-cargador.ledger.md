---
id: SPEC-004
tipo: ledger
epica: EPIC-001
---
# Ledger — SPEC-004 Calendario declarado, importador y cargador

## Resumen
- Fase: hecho (GREEN del verificador el 2026-09-21; implementación completa el 2026-09-21; H-1 y H-2 cumplidas el mismo día; calendario 2026-27 revisado y cargado en `dev`)
- Rama: `ft/SPEC-004-calendario-declarado-importador-y-cargador`

## Matriz de criterios de aceptación
<!-- Escritores: sdd-implementador rellena Implementado y Test; sdd-verificador rellena Verif. y Estado. Nunca al revés. -->
<!-- Estados por CA: ✅ cerrado · ⚠️ parcial/con salvedad · 🚧 en curso · ❌ sin empezar · n-a -->
<!-- Un CA está ✅ solo cuando Implementado + Test + Verif. aplicables están en verde. Una salvedad se marca ⚠️, nunca ✅. -->
| CA | Implementado (fichero) | Test (fichero/caso) | Verif. | Estado |
|---|---|---|---|---|
| CA-1 | `src/calendar/schema.ts` (`CalendarFile`, `CalendarMatch`, `Issue`, `validateCalendar`; claves desconocidas desdobladas en un issue por clave con su `path`); enmienda N-11: `Team.shortName?` en `src/model/entities.ts` (`min(1)`, refine `shortName !== name`), heredado por `CalendarFile.teams` | `src/calendar/schema.test.ts` «CA-1 CalendarFile» (11 casos: válido → `[]`; `providerId` → `matches.0.providerId`; `id` de partido; season/id de carpeta y fichero; equipo repetido; equipo desconocido; local = visitante; dos veces en la jornada; id derivado repetido; `round` 0; offset en `kickoff` y `teams` < 2; `shortName` opcional válido; `shortName` vacío e igual a `name` → `teams.0.shortName`); `src/model/model.test.ts` «Team shortName is optional, non-empty and different from name (N-11)» | `npm test` 212/212; `schema.test.ts` «CA-1» (14 casos) y `model.test.ts` N-11 leídos, no vacíos; `strictObject` y refine `shortName !== name` comprobados en `schema.ts`/`entities.ts`. Mutación M1 (`home` → `equipo-inexistente` en `tercera-rfef-g1.json`) → `npm test` exit 1, 2 failed (`data.test.ts` validateCalendar, `xornada.test.ts`); M2 (partido duplicado en una jornada de `segunda-rfef-g1.json`) → exit 1, 3 failed (+ N-8); ambas revertidas, árbol limpio | ✅ |
| CA-2 | `src/calendar/match-id.ts` (`matchId`); `src/model/ids.ts` (`MatchId` = slug con marca); `src/model/model.test.ts` (fixtures con `:` → formato derivado, «MatchId is a slug») | `src/calendar/match-id.test.ts` (6 casos: id exacto, no depende de `kickoff`, local/visitante invertidos, `j1` ≠ `j10`, parsea con `MatchId`, `round` 0 lanza); `src/model/model.test.ts` «MatchId is a slug» | Tres ids reales de `tercera-rfef-g1.json` contra la fórmula a mano: j1 `alondras`–`pontevedra-b`, j10 `antela`–`silva`, j34 `alondras`–`lalin` → idénticos a `matchId()` (`tercera-rfef-g1-2026-27-j10-antela-silva`, sin ceros); `MatchId` = slug con marca en `ids.ts`; `match-id.test.ts` 6 casos leídos | ✅ |
| CA-3 | `src/calendar/schema.ts` (`AliasFile`, `AliasEntry`, `validateAliases`) | `src/calendar/schema.test.ts` «CA-3 AliasFile» (6 casos: válido, `source`/`season`, `externalId` repetido, `externalName` repetido, `teamId` desconocido, clave extra) | `schema.test.ts` «CA-3» 6 casos leídos y verdes; `validateAliases` exige `source`/`season`/`externalId` y `externalName` únicos/`teamId` ∈ `knownTeams` (leído) | ✅ |
| CA-4 | `src/model/calendar-import.ts` (`ImportedCalendar`, `ImportedTeam`, `ImportedMatch`, `CalendarImporter`, `CalendarFetchContext`; reexportado por `src/model/index.ts`); `src/sources/api-football/calendar.ts` (`LEAGUES`, `apiFootballCalendar`: una petición GET con `x-apisports-key` y `User-Agent`, `parse` pura con `^(Regular Season|Group \d+) - (\d+)$`, fechas a `Z`, `timeConfirmed`); `src/calendar/importers.ts` (`IMPORTERS`, `importerFor`, `COMPETITIONS` con tier y nombre por defecto N-10) | `src/sources/api-football/calendar.test.ts` «CA-4» (3: id y `covers` de las cinco, `fetch` hace exactamente una petición a `/fixtures?league=439&season=2026` con cabeceras, rechaza competición no cubierta sin tocar la red); `src/arch/sources-boundary.test.ts` «src/sources/**/*.{ts,tsx} has no violations» en verde con el árbol real; `src/calendar/cli.test.ts` «CA-4 COMPETITIONS» | `calendar-import.ts` reexportado por `model/index.ts`; `LEAGUES` 140/141/435/875/439; `fetch` una petición GET con `x-apisports-key` y `User-Agent` (stub en `calendar.test.ts`); `parse` pura con `^(Regular Season|Group \d+) - (\d+)$`; `sources-boundary.test.ts` verde con el árbol real. Mutación M3 (`src/sources/tmp/x.ts` importando `postgres`) → `npm test` exit 1, `specifier: "postgres"`; revertida | ✅ |
| CA-5 | `src/sources/api-football/fixtures/fixtures-439-2024.json` (`Group 1 - 1..3`, 27 partidos, 18 equipos), `fixtures-141-2024.json` (`Regular Season - 1..2` + `Promotion Play-offs - final`, 24), `fixtures/README.md` (comando, fecha 2026-09-21, recorte) | `src/sources/api-football/calendar.test.ts` «CA-5» (5: `fetch` sustituido por stub que lanza; 439 rondas enteras 9/9/9, 18 equipos, `kickoff` en `Z` y `Instant`, sin rondas ignoradas; 141 22 equipos, 11 por jornada, play-off en `ignoredRounds` y no en `matches`; PST/TBD → `timeConfirmed=false` (variante en memoria del fixture real, ver F-SPEC-004-1); cuerpo no válido lanza) | Fixtures inspeccionados: 439 → `Group 1 - 1..3` 9/9/9 (`results` 27, todo `FT`), 141 → `Regular Season - 1..2` 11/11 + `Promotion Play-offs - final` 2 (`results` 24, 23 `FT` + 1 `AET`); `grep -ciE "x-ratelimit|x-apisports|api-key"` → 0 y 0; README con comando y fecha; sin red en `npm test` (stub que lanza + `env -u API_FOOTBALL_KEY`). F-SPEC-004-1 aceptada: la spec pide el `PST`/`TBD` «si existen» | ✅ |
| CA-6 | `src/calendar/sync.ts` (`syncCalendar`, `slugify`, `SyncDiff`, `formatSyncDiff`; los equipos existentes se copian enteros, `shortName` incluido, y los nuevos nacen solo con `id` y `name`) | `src/calendar/sync.test.ts` (9 casos: borrador inicial; (a) alias y sufijo `-2`; (b) `added`/`rescheduled`; (c) `missing` conservado; (d) `competition`, nombres, ids y alias intactos + `renamedAtProvider`; (d) `shortName` de equipos existentes conservado y ausente en los nuevos; (e) `unconfirmed` e `ignoredRounds`; (f) orden determinista; segunda ejecución → diff vacío; `slugify`) | `sync.test.ts` 9 casos leídos (una regla por caso a–f, idempotencia, `shortName` conservado y ausente en nuevos); `syncCalendar` copia los equipos existentes enteros y los nuevos nacen `{id, name}` (leído); sync real `--dry-run` sobre los datos revisados → diff vacío en las cinco salvo `unconfirmed 1` (F-SPEC-004-11) | ✅ |
| CA-7 | `tools/calendario-sync.mjs` (`process.loadEnvFile`, `API_FOOTBALL_KEY` obligatoria, `User-Agent: marcador.gal (calendario; https://marcador.gal)`, una petición por competición, CA-6, escritura `JSON.stringify(…, null, 2)` + salto final, `--dry-run`, exit 1 con `errors` o issues, nunca imprime la clave); `package.json` script `calendario:sync`; `formatSyncDiff` en `src/calendar/sync.ts` | `src/calendar/cli.test.ts` «CA-7» (3: sin clave → exit 1 sin red; sin temporada → exit 1; `formatSyncDiff` con contadores, listas y «REVISAR»). Ejecución real 2026-09-21 (plan Pro): `npm run calendario:sync -- 2026-27 --dry-run` → exit 0, `git status --porcelain data/` vacío, `grep -c "$API_FOOTBALL_KEY"` de la salida → 0 | `npm run calendario:sync -- 2026-27 --dry-run` → exit 0 (5 peticiones, plan Pro); `git status --porcelain data/` vacío y md5 de los 6 JSON idénticos antes/después; `grep -c "$API_FOOTBALL_KEY"` sobre la salida → 0; `cli.test.ts`: sin clave exit 1 sin red, sin temporada exit 1, `formatSyncDiff` con «REVISAR» | ✅ |
| CA-8 | `data/calendario/2026-27/{primera-division,segunda-division,primera-rfef-g1,segunda-rfef-g1,tercera-rfef-g1}.json` (tier 1..5; 380+462+380+306+306 = 1834 partidos), `data/alias/2026-27/api-football.json` (98 entradas). **Revisión H-2 de Alberto Fojo (2026-09-21) aplicada**: `name` = forma corta federativa con siglas, `shortName` en 90 equipos, `competition.name` en galego («Primeira División», «Segunda División», «Primeira Federación · Grupo 1», «Segunda Federación · Grupo 1», «Terceira Federación · Grupo 1»), 28 ids cambiados (p. ej. `coruna` → `montaneros` = Atlético Coruña Montañeros, `celta-de-vigo-iii` → `celta-c`). Acta: `tools/revision-nombres-2026-27.json`, aplicada con `tools/aplicar-revision-nombres.mjs` (idempotente: segunda ejecución «0 fichero(s) reescrito(s)»). REVISAR.md eliminado | `src/calendar/data.test.ts` (19 casos en `npm test`: cinco competiciones con tier 1..5; alias presente; por fichero CA-1 sin issues, `2(n-1)` jornadas de `⌊n/2⌋` sin equipo repetido (N-8), `name` no vacío ni igual a `id`; alias CA-3 contra la unión, biyección `teamId` ↔ alias). Verde tras la revisión (212/212 en `npm test`) | Cinco ficheros: 380+462+380+306+306 = 1834 partidos, 20/22/20/18/18 equipos, 38/42/38/34/34 jornadas; 98 equipos, 90 con `shortName`, ninguno con `shortName == name`; alias 98 entradas biyectivas; `competition.name` en galego en los cinco («Primeira División», «Terceira Federación · Grupo 1»…); `montaneros` = «Atlético Coruña Montañeros» (← «Coruña», 26534); filiales `celta-c`, `pontevedra-b`, `celta-fortuna`, `deportivo-fabril`, `bilbao-athletic`, `rayo-cantabria`, `oviedo-vetusta`, `alaves-b`, `eibar-b`, `real-sociedad-b` presentes; los 18+18 nombres de Tercera y Segunda RFEF G1 revisados uno a uno en forma corta federativa con siglas; `REVISAR.md` no existe; `data.test.ts` 19 casos en `npm test` | ✅ |
| CA-9 | `supabase/migrations/20260921084301_calendar_loads.sql` (`calendar_loads`, índice `(season, loaded_at desc)`, RLS sin políticas); `20260921090009_team_short_name.sql` (`teams.short_name text null`; `board` recreada con `home_short_name`/`away_short_name` en su sitio y `security_invoker = true`, F-SPEC-004-12) | `src/db/schema.db.test.ts` («nine tables», `calendar_loads: 0` políticas, `board` con las 22 columnas en orden y `home_short_name`/`away_short_name` nulos sin dato; 17/17 en `test:db`). Ambas migraciones aplicadas con `npm run db:push -- --yes` el 2026-09-21; `npm run db:push -- --dry-run` → `Remote database is up to date` | Migraciones leídas; `supabase db push --dry-run --db-url` → `Remote database is up to date`; `npm run test:db` 24/24 (nueve tablas, `calendar_loads` RLS sin política, 22 columnas de `board` en orden, invoker); SQL propio: `board` 22 columnas en el orden de la spec, `reloptions = {security_invoker=true}`, `calendar_loads` `relrowsecurity = true` / 0 políticas. ⚠️ F-SPEC-004-12: `drop view` + `create view` en vez de `create or replace view` (Postgres solo añade columnas al final; resultado idéntico al pedido, sin dependientes) — aceptada | ⚠️ |
| CA-10 | `src/db/connect.ts` (`createSql(env)`), `src/db/client.ts` (`import "server-only"; export const sql = createSql(process.env)`), `src/calendar/load.ts` (`loadSeason`, `LoadSummary`: upserts de `competitions`, `teams` (`name` y `short_name`, nulo si el JSON no trae `shortName`), `matches` (solo `kickoff`), `team_aliases`; `inserted/updated/unchanged/orphaned` por competición, `aliases` por fuente, fila en `calendar_loads`). `grep -niE "\bdelete\b|truncate" src/calendar/load.ts` → vacío | `src/db/calendar.db.test.ts` (7 casos en transacción con rollback, competición `test-cal`: primera carga `inserted = n` + alias + fila `calendar_loads` con el `summary`; segunda idéntica `inserted 0, unchanged n`, `count` igual; `kickoff` cambiado → `updated 1`, id igual; `shortName` añadido → `teams.short_name` y `board.home_short_name` lo muestran (`away_short_name` nulo) y quitarlo lo deja nulo; partido ausente → `orphaned 1` y la fila sigue; alias con `teamId` inexistente → `23503` y 0 partidos; `set local role anon` → `calendar_loads` = 0). 24/24 en `npm run test:db` | `connect.ts`/`client.ts` leídos (`postgres(databaseUrl(env), { ssl: "require", prepare: false })`, `server-only` solo en `client.ts`); `grep -niE "\bdelete\b|truncate" src/calendar/load.ts` → vacío; `calendar.db.test.ts` 7 casos leídos (rollback, `test-cal`, `23503`, `anon`); `test:db` 24/24; SQL propio con `set local role anon` → `calendar_loads` 0, `team_aliases` 0, `board` 1834 | ✅ |
| CA-11 | `tools/calendario-load.mjs` (lee `data/calendario/<t>/*.json` y `data/alias/<t>/*.json`, todas las temporadas si no se indica; valida todo **antes** de conectar y sale 1 listando fichero e issues; una transacción `sql.begin` por temporada; imprime `summary`); `src/calendar/files.ts` (`readSeasons`); `package.json` script `calendario:load` | `src/calendar/files.test.ts` (3: lectura de varias temporadas y filtro, issues con fichero y orden, temporada inexistente); `src/calendar/xornada.test.ts` «CA-11» (2: sin `DATABASE_URL` valida `data/` y sale 1 sin issues; temporada inexistente sale 1 sin tocar la base). Ejecución real 2026-09-21 con temporada temporal `2024-25`/`test-cal` (12 partidos, 4 alias) contra `dev`: 1.ª carga `inserted 12`; 2.ª `inserted 0, unchanged 12`; `count(matches)` = `count(board)` = 12; `calendar_loads` = 2; `team_aliases` = 4; revertida después (todas las tablas a 0, ficheros borrados). **Real, 2026-09-21 tras H-2:** `npm run calendario:load -- 2026-27` ×2 contra `dev`: 1.ª `inserted` 380/462/380/306/306 (`aliases.api-football: 98`); 2.ª `inserted 0`, `unchanged` 380/462/380/306/306, `orphaned []` en las cinco. SQL: `count(matches)` = 1834 = suma de los cinco ficheros = `count(board)`; `calendar_loads where season='2026-27'` = 2; `team_aliases where source_id='api-football'` = 98 = entradas del alias; `teams` = 98, 90 con `short_name` | `npm run calendario:load -- 2026-27` ×2 → exit 0, `inserted 0`, `unchanged` 380/380/462/306/306, `orphaned []`, `aliases.api-football 98` en ambas; SQL: `count(matches)` 1834 = suma JSON = `count(board)`; `calendar_loads where season='2026-27'` = 5 (≥ 2); `team_aliases where source_id='api-football'` = 98; `teams` 98, 90 con `short_name`, nulos exactamente `bilbao-athletic, celta-fortuna, ourense-cf, rayo-cantabria, real-madrid, real-sociedad, real-sociedad-b, ud-ourense`; `board.home_short_name` no nulo en 1682 = 1834 − 152 (partidos en casa de esos 8); `montaneros` en `board` con `home_short_name` «Montañeros»; 0 partidos con equipo sin fila; `files.test.ts`/`xornada.test.ts` leídos | ✅ |
| CA-12 | `src/calendar/current-round.ts` (`currentRound`: mediana por jornada, más cercana a `now`, menor a igual distancia, `[]` → `null`); `tools/calendario-xornada.mjs` (por competición de la temporada: `round, kickoff` de `matches`, `currentRound`, consulta de `board` ordenada por `kickoff, home_team_id`, imprime competición/jornada/filas, sale 1 si alguna da 0 filas); `package.json` script `calendario:xornada` | `src/calendar/current-round.test.ts` (8: `[]` → null; antes → 1; después → 5; martes tras la 2 → 2; jueves antes de la 3 → 3; lunes 21:00 → 2; aplazado de la 1 a la fecha de la 5 con `now` en la 4 → 4; mediana impar; empate → menor); `src/calendar/xornada.test.ts` «CA-12» (sin `DATABASE_URL` → 1). Ejecución real con `test-cal` (2024-25): «xornada 6 — 2 partidos», exit 0. **Real, 2026-09-21 tras H-2:** `npm run calendario:xornada -- 2026-27` → exit 0, 49 filas: `primera-division (Primeira División) — xornada 7 — 10 partidos`, `segunda-division (Segunda División) — xornada 6 — 11 partidos`, `primera-rfef-g1 (Primeira Federación · Grupo 1) — xornada 4 — 10 partidos`, `segunda-rfef-g1 (Segunda Federación · Grupo 1) — xornada 3 — 9 partidos`, `tercera-rfef-g1 (Terceira Federación · Grupo 1) — xornada 3 — 9 partidos` (= `⌊n/2⌋` en las cinco; criterio 4 de EPIC-001) | `currentRound` a mano sobre `tercera-rfef-g1.json`: medianas j2 = 2026-09-13T10:30Z, j3 = 2026-09-20T16:00Z; martes 2026-09-15T10:00Z → 2 (47,5 h frente a 126 h de j3), jueves 2026-09-17T10:00Z → 3 (78 h frente a 95,5 h de j2); `current-round.test.ts` 8 casos leídos; `npm run calendario:xornada -- 2026-27` → exit 0, cinco competiciones con 10/11/10/9/9 filas = ⌊n/2⌋ (criterio 4 de EPIC-001), filas con kickoff, local, visitante y `[scheduled]` | ✅ |
| CA-13 | `src/calendar/index.ts` (reexporta schema, match-id, sync, load, importers, current-round); imports relativos con `.ts` en `src/model/*.ts`, `src/calendar/*.ts`, `src/db/env.ts`, `src/db/connect.ts`, `src/db/client.ts`, `src/sources/api-football/calendar.ts`; `package.json` solo añade los tres scripts (`dependencies`/`devDependencies` sin cambios, `git diff main -- package.json`); `.env.example` sin cambios | `node --input-type=module -e "await import('./src/calendar/index.ts')"` → exit 0 (aviso `MODULE_TYPELESS_PACKAGE_JSON`, F-SPEC-004-3); bucle `git grep -qF` con cada valor de `.env` sobre `HEAD` → 0 fugas; fixtures sin clave ni cabeceras de respuesta (`grep -ciE "x-ratelimit|x-apisports"` → 0); clon limpio (`git clone` a un directorio temporal sin `.env`) `env -u DATABASE_URL -u API_FOOTBALL_KEY npm ci && npm run gates` → exit 0 (2026-09-21, Node 26.4.0) | `node --input-type=module -e "await import('./src/calendar/index.ts')"` → exit 0 (aviso F-SPEC-004-3); grep de imports relativos sin `.ts` y de sintaxis no borrable en los ficheros listados → ninguno; `git diff main -- package.json` → solo los tres scripts; `.env.example` sin diff; `npm ci && env -u DATABASE_URL -u API_FOOTBALL_KEY npm run gates` → exit 0 (Biome 72 ficheros, Vitest 19 ficheros / 212, `next build` ok; Node 26.4.0); bucle `git grep -qF` de los 8 valores de `.env` sobre `HEAD` y sobre el árbol → 0 fugas; PR #4, run 35583352584 sobre `80d1770`: `gates` pass (29 s) y `e2e` pass (50 s) con Node 24.20.0 (`.nvmrc`) | ✅ |

## Veredicto del verificador
<!-- GREEN/RED + fecha + resumen. Lo escribe SOLO sdd-verificador. -->
**GREEN** — 2026-09-21, sdd-verificador, sobre `80d1770`. 12 CA ✅, CA-9 ⚠️ (F-SPEC-004-12 aceptada). Solo artefactos: spec, ledger, código, tests, datos, `dev` y CI; ningún relato del implementador se ha tomado como evidencia.

- **Gates y regresión**: `npm ci && env -u DATABASE_URL -u API_FOOTBALL_KEY npm run gates` exit 0 (212/212, 19 ficheros); `npm run test:db` 24/24 contra `dev`; `supabase db push --dry-run` al día; PR #4 `gates` y `e2e` en verde con Node 24.20.0.
- **Flujo real contra `dev`**: sync `--dry-run` (5 peticiones, exit 0, `data/` intacto, clave no impresa) → load ×2 idempotente (`inserted 0`) → xornada con las cinco competiciones a ⌊n/2⌋ partidos. Consultas SQL propias en la fila de CA-11.
- **Mutaciones propias** (las tres revertidas, árbol limpio): equipo inexistente → 2 failed; partido duplicado → 3 failed; `src/sources/tmp/x.ts` con `postgres` → 1 failed en `sources-boundary.test.ts`.
- **Datos (H-2)**: revisados como aficionado gallego; 98 nombres en forma corta federativa, filiales con sufijo, `montaneros` correcto, competiciones en galego, ningún `shortName` igual a su `name`, `REVISAR.md` ausente. Observación sin efecto en CA: la jornada 5 de Tercera trae los 9 partidos a `2026-10-04T16:00Z` (hora provisional del proveedor); N-1 lo cubre y el sync semanal la corregirá.
- **Salvedades F-SPEC-004-1..13 valoradas**: ninguna rompe un CA. F-1, F-2, F-5, F-6, F-7, F-8, F-9, F-10, F-11, F-13 informativas (F-13: `tools/aplicar-revision-nombres.mjs` y `tools/revision-nombres-2026-27.json` solo importan `node:*`, pasan Biome y son el acta de H-2; se quedan). F-3 (aviso `MODULE_TYPELESS_PACKAGE_JSON`) reproducido, exit 0, destino EPIC-MEJORA. F-4 cerrada con evidencia propia (diff vacío tras la revisión). F-12 aceptada como ⚠️ en CA-9: desviación literal justificada, artefacto resultante idéntico (22 columnas en orden, `security_invoker`).

## Evidencia visual
<!-- Tabla CA → captura en _qa/SPEC-004/. Informe HTML opcional: _qa/SPEC-004/informe.html -->
n-a: la spec no tiene interfaz; la evidencia es la salida de comandos y consultas SQL anotada en la matriz.

## Salvedades / follow-ups
<!-- IDs F-SPEC-004-1, F-SPEC-004-2… con destino (spec futura o EPIC-MEJORA). -->
- **F-SPEC-004-1** CA-5: la temporada 2024 estaba terminada en la captura (todo `FT`/`AET`), así que no existe ningún partido `PST`/`TBD` real en los fixtures; el test deriva ese caso cambiando `status.short` en memoria sobre un partido real. Además, la liga 439 nombra sus rondas `Group 1 - N` (no `Regular Season - N`): cubierto por la expresión de CA-4. Informativo.
- **F-SPEC-004-2** CA-5: el fixture 141 incluye los dos partidos de `Promotion Play-offs - final` (ida y vuelta) como ronda no regular; `ignoredRounds` cuenta 2. Informativo.
- **F-SPEC-004-3** Los tres comandos y el import de `src/calendar/index.ts` en Node emiten el aviso `MODULE_TYPELESS_PACKAGE_JSON` (mismo caso que F-SPEC-003-1; `package.json` sigue sin `"type"`). Destino: EPIC-MEJORA.
- **F-SPEC-004-4** **Cerrada (2026-09-21).** El borrador del sync se revisó (H-2, Alberto Fojo: `tools/revision-nombres-2026-27.json`) y se cargó en `dev`; evidencia en CA-8, CA-11 y CA-12. `npm run calendario:sync -- 2026-27 --dry-run` tras la revisión: exit 0, `data/` sin cambios, en las cinco competiciones `newTeams 0, added 0, rescheduled 0, missing 0, renamedAtProvider 0` (el alias conserva los `externalName` del proveedor, por eso 0), `unconfirmed 1` (F-SPEC-004-11); el sync no propone cambios de `name`/`shortName`. Cerrada.
- **F-SPEC-004-5** Fuera del texto literal de la spec, necesarios para los comandos y sus tests: `formatSyncDiff` (en `sync.ts`), `COMPETITIONS` e `importerFor` (en `importers.ts`), `src/calendar/files.ts` (`readSeasons`, lectura+validación de `data/<temporada>`, compartida por `calendario:load`), y `.ts` explícito también en los imports de los tests. `src/calendar/index.ts` no reexporta `files.ts` (no está en CA-13). Informativo.
- **F-SPEC-004-6** CA-6 (a): un equipo con alias cuyo `teamId` no está en el calendario actual (p. ej. alias revisado a mano antes del primer sync) se añade a `teams` con el nombre del proveedor y va a `diff.newTeams` para que se revise su `name`; la spec solo define el caso «sin alias». `diff.newTeams` sale ordenado por `teamId`. Informativo.
- **F-SPEC-004-7** CA-7: si una competición falla (errores del proveedor o issues de CA-1), el comando sigue con las demás, escribe las que validan y sale 1 al final; el alias se escribe solo si valida contra la unión de calendarios de la temporada. Con `--dry-run` no escribe nada en ningún caso. Informativo.
- **F-SPEC-004-8** CA-12: `calendario:xornada` sin temporada usa la temporada en curso por fecha (julio como corte); la spec deja `[temporada]` opcional sin fijar el valor por defecto. Informativo.
- **F-SPEC-004-9** `CalendarImporter.covers` de API-Football ignora la temporada (cubre las cinco ligas en cualquier temporada); la disponibilidad real depende del plan (Free: 2021-2024; Pro: 2026 comprobado). Informativo.
- **F-SPEC-004-10** Gates, `test:db` y comandos ejecutados en local con Node 26.4.0 (`.nvmrc` 24, sin Node 24 instalado), como F-SPEC-002-9. Push de la rama por instrucción explícita del orquestador (sin PR ni merge), como F-SPEC-002-6. Informativo.
- **F-SPEC-004-13** `tools/aplicar-revision-nombres.mjs` y `tools/revision-nombres-2026-27.json` son el acta y la herramienta de la revisión H-2 (script puntual, fuera de los CA, sin dependencias); se quedan en `tools/` como constancia. Informativo.
- **F-SPEC-004-12** Enmienda N-11 (CA-9): la spec pide `create or replace view board` con `home_short_name` tras `home_team_name`, pero Postgres solo permite a `create or replace view` añadir columnas al final; la migración hace `drop view public.board` + `create view` con las 22 columnas en el orden pedido y `security_invoker = true` (los tests de `board` y de lectura `anon` siguen en verde). Ninguna dependencia de la vista existía. Informativo.
- **F-SPEC-004-11** El sync marca un partido con hora sin confirmar: `primera-division-2026-27-j6-levante-athletic-club` (`TBD` en el proveedor); el JSON guarda la fecha que dio el proveedor (N-1) y el próximo sync la corregirá. Anotado en REVISAR.md. Informativo.

## Cómo retomar (handoff)
<!-- Estado real del trabajo para la siguiente sesión: qué está hecho, qué falta, dónde seguir. -->
- **Hecho (2026-09-21, rama `ft/SPEC-004-calendario-declarado-importador-y-cargador`):** los 13 CA implementados con test, incluida la enmienda N-11 (`Team.shortName`, `teams.short_name`, `board.home_short_name`/`away_short_name`, cargador); `npm test` 212/212 (16 ficheros), `npm run test:db` 24/24 contra `dev`, `npm run typecheck` y `npm run lint` limpios; migraciones `calendar_loads` y `team_short_name` aplicadas en `dev` (`db:push --dry-run` al día). API-Football ya es Pro (límite 7500): el sync real de 2026-27 está hecho y commiteado como borrador.
- **H-1 y H-2 cumplidas (2026-09-21):** plan Pro activo; revisión de nombres de Alberto Fojo aplicada (`tools/revision-nombres-2026-27.json` + `tools/aplicar-revision-nombres.mjs`); `dev` tiene cargado el calendario 2026-27 (1834 partidos, 98 equipos, 98 alias, 2 filas en `calendar_loads`). Resincronizar cada semana con `npm run calendario:sync -- 2026-27` y volver a `npm run calendario:load -- 2026-27` (la carga es idempotente).
- **Para el verificador:** reproducir con `npm run gates`, `npm run test:db`, `npm run calendario:sync -- 2026-27 --dry-run` (5 peticiones; con Pro hay margen), el bucle `git grep -qF` de secretos, `node --input-type=module -e "await import('./src/calendar/index.ts')"`, `npm run db:push -- --dry-run`. Las verificaciones reales de CA-11/CA-12 están hechas contra `dev` (ver matriz); `calendario:load` puede repetirse sin efecto (`inserted 0`).
